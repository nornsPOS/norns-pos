# GDPR Art.17 Erasure — Definitive Build Spec (2026-06-29)

Generated by the PII-mapping review. The build blueprint for C1 (customer erasure) + C4 (KYC replace/delete).

The three core tables confirm the spec precisely. The maps are accurate and comprehensive. I have what I need to write the definitive plan — the spec's CHECK constraints, NOT NULL columns, and grant gaps all verify against source. Let me produce the build spec.

I have everything I need. The three load-bearing tables (`customers`, `shoppers`, `kyc_documents`) verify exactly against the maps — every CHECK, NOT NULL, and partial-unique cited is correct. Below is the definitive erasure plan.

---

# GDPR Art.17 Erasure — Definitive Build Spec (warehouse14)

## Model: ANONYMIZE-IN-PLACE, never row-delete

The entire system is built so the subject's identity rows (`customers`, `shoppers`) survive as anonymized shells; only PII *content* is scrubbed. Fiscal/GoBD/GwG rows are kept verbatim with embedded PII NULLed. Two cleanly-deletable surfaces exist (`shopper_sessions`). Everything else is scrub-in-place.

## Two entry keys
- **Dealer customer:** drive by `customers.id`.
- **Storefront shopper:** drive by `shoppers.id`, then ALSO run the full customer erasure on that shopper's `customers.customer_id` (1:1, `shoppers.customer_id` NOT NULL UNIQUE — `shoppers.ts:37-40`). Erasing a shopper without erasing its `customers` row leaves the KYC/PII master intact.
- **Phone-keyed sweeps (whatsapp / intake):** most of these rows have no customer FK. Resolve the subject's phone (decrypt `customers.phone_encrypted` or read `whatsapp_conversations.customer_phone_e164` where `customer_id` matches) and sweep by phone string. Intake has NO customer link at all — it must be driven by a separate session/phone retention sweep, not this transaction.

## Roles required
The app role (`warehouse14_app`) is deliberately barred from most of these writes. The erasure job MUST run as a **privileged owner role** (or `SECURITY DEFINER` functions) for: `customers` blind-index/encrypted cols where not granted, `kyc_documents` purge, `appointment_notifications.recipient`, all `whatsapp_*` raw-PII cols, `transactions.shipping_address_encrypted`, `transaction_items`, `products` intake-locked rows, `shoppers.email_encrypted`/`email_blind_index`, `mcp_tool_invocations`, `webhook_events`. Strictly-immutable tables (`ledger_events`, `tse_*`) reject ALL mutation even from owner via triggers — those are never touched.

---

## ORDERED OPERATION LIST (single transaction unless noted)

### Phase 0 — Out-of-band file deletes (schedule; commit DB first, then unlink)
These are object-store/disk bytes the DB only references by key. Collect keys inside the transaction, perform the actual unlink AFTER commit (idempotent, retry-safe):
1. **KYC image files** under `KYC_PHOTOS_DIR` (migration 0074) for every `kyc_documents.document_photo_storage_key` where `customer_id = :subject` — via the app's `deleteKycImage` path.
2. **R2 objects**: `document_attachments.r2_key` (subject rows), `appraisal_items.photo_r2_keys[]`, `intake_messages.media_r2_key`, `intake_drafts.bg_removed_photo_keys[]` for correlated sessions.
3. **Flag-only (cannot scrub):** already-generated DSFinV-K bundles (`dsfinvk_exports.r2_key`) and TSE daily archives (`tse_daily_archives.r2_key`) may embed the subject's shipping address inside the exported artifact. GoBD 10-year retention overrides Art.17 — document this as a lawful retention exception, do not attempt deletion.

### Phase 1 — Delete-row (only one table qualifies)
4. **`shopper_sessions`** — `DELETE WHERE shopper_id = :shopper`. No fiscal value; kills live logins; app+worker both have DELETE grant (0018:420/456). (Only applies to the shopper entry path.)

### Phase 2 — Scrub child/linked PII (do BEFORE the customers/shoppers master, so a partial failure never leaves the master flagged-anonymized while children still hold PII)

**Appointments cluster** (sweep `WHERE customer_id = :subject` AND walk-in rows whose `contact_*` match the subject):
5. **`appointments`** → NULL `contact_name, contact_phone, contact_email, customer_notes, staff_notes, cancellation_reason`. All nullable. App has the grant (0062:75). Status stays unchanged → the BEFORE-UPDATE transition validator (0012:386) early-returns. Never write `ends_at` (GENERATED, `appointments.ts:39`).
6. **`appointment_notifications`** (reach via `appointment_id` of subject's appointments) → set `external_ref = NULL` (app-grantable); overwrite `recipient = 'REDACTED'` (NOT NULL, **privileged role only** — no app UPDATE grant on `recipient`).
7. **`product_viewing_holds`** → NO column write. `released_reason` is coupled to `released_at` by the `released_has_reason` all-or-nothing CHECK; the `customer_id` link is neutralized by the kept-anonymized customers shell. Leave as-is.

**WhatsApp cluster** (resolve subject phone; sweep by `from_phone`/`to_phone`; **all privileged role**):
8. **`whatsapp_conversations`** → set `anonymized_at = now()`, NULL `customer_id`, and overwrite `customer_phone_e164 = 'erased:' || id` (NOT NULL + UNIQUE → per-row token, never a shared constant). Keep the row (it's the join key already used by the sweep — sweep first, then redact).
9. **`whatsapp_inbound_messages`** (`WHERE from_phone = :phone`) → NULL `body_encrypted`, NULL `processing_error`; set `raw_payload = '{}'::jsonb` (CHECK `whatsapp_inbound_payload_object` forbids NULL/scalar); overwrite `from_phone = 'REDACTED'` (NOT NULL).
10. **`whatsapp_outbound_messages`** (`WHERE to_phone = :phone`) → set `body = '–'` (CHECK `whatsapp_outbound_body_nonempty` + NOT NULL → single placeholder char, not '' / NULL), NULL `body_encrypted`, NULL `template_params`. For `provider_error`: if `status='failed'`, replace with a stripped object (CHECK `whatsapp_outbound_error_status_check` ties failed→`provider_error IS NOT NULL`); else NULL. Overwrite `to_phone = 'REDACTED'` (NOT NULL).
11. **`ai_calls`** → no action (cost ledger, no PII; FK to the kept conversation stays).

**Appraisals / products cluster:**
12. **`appraisals`** (`WHERE customer_id = :subject`, NOT NULL FK → row kept) → NULL `notes`. For `rejection_reason`: if `status='REJECTED'`, overwrite with `'GELOESCHT'` (CHECK `appraisals_rejected_has_reason`), else NULL. ACCEPTED rows stay (fiscal evidence via `ankauf_transaction_id`).
13. **`appraisal_items`** (reach via parent appraisal) → NULL `notes, description`; set `photo_r2_keys = '{}'`. Leave `name` (item label, not PII).
14. **`products`** (`WHERE acquired_from_customer_id = :subject`; **privileged role** — intake-locked grant exclusion) → NULL `provenance_notes`; NULL `acquired_from_customer_id` (nullable, no CHECK). Never touch `sku/barcode/acquisition_cost_eur` (§25a-immutable).
15. **`vouchers`** (`WHERE issued_to_customer_id = :subject`) → NULL `notes` (app-grantable, 0019:503). Leave `issued_to_customer_id` (not in app grant; neutralized by the anonymized customers shell). Keep the row (§3(14) UStG). `voucher_redemptions` → untouched (immutable, no PII).

**Documents cluster:**
16. **`document_attachments`** (`WHERE customer_id = :subject`) — keep-fiscal shell, **do NOT NULL `customer_id`** (the `exactlyOneLink` CHECK + `ausweis_is_customer` / ankaufbeleg-rechnung category CHECKs require the link). Per row: overwrite `r2_key` and `file_name` with short sentinels (both NOT NULL, length CHECKs 1..1024 / 1..255 → can't NULL); NULL `sha256_hex` (format CHECK allows NULL); NULL `notes`; set `archived_at = now()`. (R2 object deleted in Phase 0.)
17. **`kyc_documents`** (`WHERE customer_id = :subject`; **privileged role**) — apply the established GDPR #I-5 purge, ALL-OR-NOTHING per the `purged_consistency` CHECK: in ONE update set `document_number_encrypted = NULL`, `document_photo_sha256 = NULL`, `document_photo_storage_key = NULL`, `document_photo_size_bytes = NULL`, `purged_at = now()`, `purged_by_user_id = :actor` (NOT NULL in purged branch — supply the erasure actor's user id). Never a partial NULL. Keep the row (GwG/§259 evidence shell); keep `issued_on/expires_on/document_type/issuing_country`.

**Audit / Fernwerkzeug-Protokoll / tasks (keep-row security evidence):**
18. **`internal_tasks`** (`WHERE related_entity_table='customers' AND related_entity_id = :subject`) → clear the pointer as a PAIR (`related_entity_table = NULL, related_entity_id = NULL` together — CHECK `..._both_or_none`); overwrite `title` with a placeholder (NOT NULL, len 1..200); NULL `description`; for `cancellation_reason` keep ≥4 chars if `status='CANCELLED'` else NULL. App has no delete (forensic/GoBD).
19. **`mcp_tool_invocations`** (`WHERE affected_entity_table IN ('customers','shoppers') AND affected_entity_id = :subject`; **privileged role**) → set `arguments = '{}'::jsonb` (NOT NULL); for `result`, if `outcome='SUCCESS'` replace with a redacted `{}` object (CHECK requires NOT NULL on success) else NULL; NULL `error_message`.
20. **`audit_log`** — keep-row, no mutation in this transaction. App/owner have no UPDATE on `payload` (append-only). Rely on the existing no-raw-PII-in-payload discipline; actor IP masked at 180d by the existing `gdpr-cleanup` worker. If a payload is ever found to hold raw subject PII, that is a separate privileged migration, not this flow.

**Storefront fiscal-linked (keep-row):**
21. **`payment_intents`** (correlate via cart→shopper) → review `outcome`; if it carries payer PII replace with `'{}'::jsonb` (CHECK `..._outcome_is_object` forbids NULL); scrub `client_secret/redirect_url` if they embed identifiers. App-grantable (0018:434-438).
22. **`webhook_events`** — **erasure gap, flag explicitly.** No shopper FK; must content-correlate by `provider_event_id`/intent id. `raw_body` (NOT NULL text) + `payload` (NOT NULL jsonb object) → overwrite with redacted constant / `'{}'`. App grant is `processed_at, processing_error` only → **privileged role**. Document as a known fragile-correlation gap.
23. **`carts`** — no action (no PII; converted carts are fiscal-linked and must not be deleted; `shopper_id` neutralized by anonymized shopper shell). `cart_items` — no action.

**Fiscal master (keep-fiscal, embedded PII only, privileged role):**
24. **`transactions`** (`WHERE customer_id = :subject`) → NULL `shipping_address_encrypted` (**no app grant — privileged/SECURITY DEFINER only**, 0018:447); scrub `notes_internal` if it holds buyer PII (app-grantable). **Never NULL `customer_id`** — the `trg_transactions_validate_storno` trigger (0009:301) requires a storno to match the original customer; clearing it breaks the invariant.
25. **`transaction_items` / `transaction_payments`** — keep-fiscal, no mutation. INSERT-only immutable; `line_discount_reason` only scrubbed by privileged role if an audit finds embedded PII; payments are masked-PAN only.
26. **`tse_signatures`, `tse_transactions`, `tse_daily_archives`, `tse_clients`, `ledger_events`** — **NEVER TOUCHED.** Triggers hard-refuse mutation (BSI TR-03153 / §259 StGB / GoBD hash-chain). Any write would break `verify_ledger_chain()`. Excluded from the UPDATE/DELETE set entirely.
27. **`daily_closings`, `dsfinvk_exports`, `operating_expenses`, `fixed_costs`, `system_settings`, `shifts`, `cash_movements`, `belegtext_templates`, `metal_prices`, reference tables, `worker_*`, `auth.*`** — no customer PII / out of scope. No action.

### Phase 3 — Stamp the master rows LAST

28. **`shoppers`** (shopper path; **privileged role** for the email cols) — ONE update:
    - Overwrite `email_encrypted = <encrypted tombstone>`, `email_blind_index = <fixed tombstone hmac>` (both NOT NULL → can't NULL; **no app grant — privileged**).
    - NULL `phone_encrypted, phone_blind_index, all shipping_*_encrypted, billing_*_encrypted, shipping_country, billing_country, email_verification_token`.
    - `marketing_consent = FALSE` (then `marketing_consent_at` may stay/NULL — CHECK `..._has_timestamp`).
    - Credential CHECK `shoppers_has_credential` (≥1 of password/google must be non-NULL): set `password_hash = <random non-null hash>`, `google_sub = NULL`.
    - Set `soft_deleted_at = now()` then `anonymized_at = now()` (CHECK `shoppers_anonymized_implies_soft_deleted`). Setting `soft_deleted_at` lifts `shoppers_email_blind_active_uq` and `shoppers_google_sub_active_uq`, freeing re-signup.

29. **`customers`** — the core anonymize, ONE update, in this exact shape to satisfy all CHECKs simultaneously:
    - `full_name_encrypted = encrypt_pii('GELOESCHT')` (NOT NULL → tombstone, never NULL).
    - NULL `date_of_birth_encrypted, email_encrypted, phone_encrypted, address_encrypted, notes_encrypted` (DOB is documented-immutable but has no DB CHECK; erasure is the lawful exception).
    - NULL `email_blind_index, phone_blind_index` (true erasure — even though `soft_deleted_at` already lifts the active-unique partial indexes).
    - NULL `vat_id`; set `customer_tags = '{}'`; NULL `price_expectation_notes`.
    - **Reset `trust_level = 'NEW'`** in the same UPDATE — CHECK `customers_banned_or_suspicious_has_note` requires `price_expectation_notes ≥ 8 chars` when trust is SUSPICIOUS/BANNED; NULLing the note while trust stays SUSPICIOUS/BANNED FAILS. (`'NEW'` also keeps `customers_verified_trust_requires_kyc` satisfied.)
    - Set `soft_deleted_at = now()` then `anonymized_at = now()` — order enforced by `customers_anonymized_implies_soft_deleted` (152) and `customers_anonymized_after_soft_deleted` (156). Equal timestamps satisfy `>=`.
    - **Do NOT write** `cumulative_spend_eur / cumulative_ankauf_eur / cumulative_debt_eur` (trigger-only; app has no grant) or `customer_number` (kept as fiscal/audit pseudonym).

---

## WHAT STAYS (and why it's lawful)
- **All fiscal/GoBD/GwG rows**: `transactions`, `transaction_items`, `transaction_payments`, `tse_*`, `ledger_events`, `daily_closings`, `dsfinvk_exports`, `vouchers`+`voucher_redemptions`, `kyc_documents` (as shells), `document_attachments` (fiscal categories, as shells). **One-line justification:** §147 AO / §257 HGB / GoBD impose a 10-year immutable retention that overrides Art.17 (Art.17(3)(b) GDPR — legal obligation); the records are kept, only PII *embedded* in them is redacted.
- **`customer_number`** and the anonymized `customers`/`shoppers`/`whatsapp_conversations` shell rows: kept for fiscal-join integrity and to make every FK-by-value reference still resolve (no cascade orphaning).

---

## NEW MIGRATIONS REQUIRED (build prerequisites)
None relax a CHECK (every CHECK above is satisfiable with the sentinel/ordering tactics). What IS needed is **GRANT scope** — either add these grants to the privileged erasure role, or wrap the writes in `SECURITY DEFINER` functions owned by the table owner:

1. **Grant UPDATE (or SECURITY DEFINER fn) for the erasure role on:** `customers.email_blind_index`, `customers.phone_blind_index` (if app-driven), `appointment_notifications.recipient`, `whatsapp_inbound_messages.from_phone`+`raw_payload`+`processing_error`, `whatsapp_outbound_messages.to_phone`+`body`+`template_params`+`provider_error`, `whatsapp_conversations.customer_phone_e164`, `transactions.shipping_address_encrypted`, `transaction_items.line_discount_reason`, `products` intake-locked rows (`provenance_notes`, `acquired_from_customer_id`), `shoppers.email_encrypted`+`email_blind_index`, `mcp_tool_invocations` (append-only — needs UPDATE), `webhook_events.raw_body`+`payload`, `audit_log.payload` (only if a real PII-in-payload case is confirmed).
2. **Decision needed, no migration yet:** `webhook_events` and `audit_log.payload` have no subject FK — they require a content-correlation strategy, not just a grant. Flag both as residual erasure gaps to the data-protection owner.

---

## KEY GOTCHA INDEX (the transaction must handle every one)
- NOT NULL → tombstone, never NULL: `customers.full_name_encrypted`, `shoppers.email_encrypted`/`email_blind_index`, `appointment_notifications.recipient`, `whatsapp_*.from_phone`/`to_phone`, `whatsapp_outbound.body`, `whatsapp_conversations.customer_phone_e164`, `document_attachments.r2_key`/`file_name`, `internal_tasks.title`.
- UNIQUE + NOT NULL → per-row token: `whatsapp_conversations.customer_phone_e164`, `intake_messages.whatsapp_message_id` (`'erased:'||id`).
- JSONB-object CHECK → set `'{}'`, never NULL: `whatsapp_inbound.raw_payload`, `payment_intents.outcome`, `webhook_events.payload`, `mcp_tool_invocations.arguments`/`result`(SUCCESS).
- Conditional-NOT-NULL CHECK → keep a placeholder, not NULL: `appraisals.rejection_reason` (REJECTED→'GELOESCHT'), `whatsapp_outbound.provider_error` (failed→stripped object), `internal_tasks.cancellation_reason` (CANCELLED→≥4 chars).
- Coupled all-or-nothing CHECK → set the whole group together: `kyc_documents` purge (5 cols + actor), `document_attachments` link CHECKs (keep `customer_id`), `internal_tasks` related-entity pair, `customers` trust↔note (reset `trust_level='NEW'`), `shoppers` has-credential (random hash + NULL google_sub).
- Ordering CHECKs → `soft_deleted_at` then `anonymized_at`, equal-or-after: `customers`, `shoppers`.
- Immutable-by-trigger → never in the write set: `tse_*`, `ledger_events`, `transaction_items`/`payments` (INSERT-only).
- Storno invariant → never NULL `transactions.customer_id`.
- Orphan-PII (no customer FK, missed by id-driven erasure) → separate session/phone sweep: `intake_sessions`, `intake_messages`, `intake_drafts`, `whatsapp_*` (phone-keyed), `webhook_events` (content-correlated).

Files cited (all verified): `customers/customers.ts:48-196`, `storefront/shoppers.ts:33-123`, `customers/kycDocuments.ts:45-143`; plus the per-domain maps for all other tables.

---

## Full per-table PII map

- **customers** [null-pii] (packages/db/src/schema/customers/customers.ts:48) — link: id (this IS the customer/data-subject row; PK) — PII: full_name_encrypted, date_of_birth_encrypted, email_encrypted, phone_encrypted, address_encrypted, notes_encrypted, email_blind_index, phone_blind_index, price_expectation_notes, vat_id, customer_tags
  - gotchas: THE core anonymize-in-place target. NEVER delete the row (table comment line 10). full_name_encrypted is NOT NULL (line 61) so you cannot NULL it; the convention is to overwrite with an encrypted tombstone (e.g. encrypt_pii('GELOESCHT')) rather than NULL. MUST clear BOTH blind indexes (email_blind_index/phone_blind_index, lines 69-70) or the customer stays re-identifiable AND the partial unique indexes customers_email_blind_index_active_uq / customers_phone_blind_index_active_uq (lines 123-128) keep colliding — note those uniques are scoped `WHERE soft_deleted_at IS NULL`, so setting soft_deleted_at also lifts the uniqueness, but you should still NULL the blind indexes for true erasure. date_of_birth_encrypted is documented IMMUTABLE after first capture (line 11) — erasure is the legitimate exception; it has no DB CHECK blocking the NULL. KEEP customer_number (line 54, NOT NULL, fiscal/audit pseudonym) and the trigger-maintained cumulative_* / debt columns. Set soft_deleted_at THEN anonymized_at and respect the two ordering CHECKs: customers_anonymized_implies_soft_deleted (anonymized_at requires soft_deleted_at, line 152) and customers_anonymized_after_soft_deleted (anonymized_at >= soft_deleted_at, line 156). trust_level CHECK customers_banned_or_suspicious_has_note (line 183) requires price_expectation_notes >= 8 chars when trust_level is SUSPICIOUS/BANNED — if you NULL price_expectation_notes you must also reset trust_level to a non-SUSPICIOUS/BANNED value (e.g. NEW) in the same UPDATE or the CHECK fails. App role has no UPDATE on cumulative_* columns (trigger-only).
- **kyc_documents** [keep-fiscal] (packages/db/src/schema/customers/kycDocuments.ts:45) — link: customer_id (NOT NULL FK -> customers.id, line 49) — PII: document_number_encrypted, document_photo_storage_key, document_photo_sha256, document_photo_size_bytes, issuing_authority, issued_on, expires_on, document_type, issuing_country_iso2
  - gotchas: GwG / §259 StGB legal-evidence rows — NEVER hard-deleted by app role (line 13). Already has a purpose-built purge mechanism (GDPR #I-5, migration 0041): the row becomes a SHELL by NULLing document_number_encrypted + document_photo_sha256 + document_photo_storage_key + document_photo_size_bytes and stamping purged_at + purged_by_user_id. CRITICAL: the kyc_documents_purged_consistency CHECK (line 126) is all-or-nothing — it requires that EITHER everything-present+purge-NULL OR all-three-PII-NULL+purge-stamped; a partial NULL (e.g. only the photo key) violates it, and purged_by_user_id is NOT NULL in the purged branch so you must supply an actor user id. The separate kyc_documents_sha256_length CHECK (octet_length=32) is written WITHOUT an IS NULL guard (line 112) — when document_photo_sha256 is NULL, octet_length returns NULL and the CHECK passes (NULL is not FALSE), so it does not block the purge, but do not 'fix' it to a strict form. App role only has UPDATE on the purge/verification/retention columns (migration 0041 GRANT) — the document core is INSERT-once. The actual encrypted image FILE on disk (KYC_PHOTOS_DIR, migration 0074) must be unlinked by the app's deleteKycImage path; the DB purge only nulls the storage key. issued_on/expires_on/document_type/issuing_country are ID-document attributes (quasi-PII) but are kept as part of the evidence shell — only the three CHECK-listed columns are nulled by the established purge.
- **appointments** [null-pii] (packages/db/src/schema/appointments/appointments.ts:28) — link: customer_id (nullable FK -> customers.id, line 43) — PII: contact_name, contact_phone, contact_email, customer_notes, staff_notes, cancellation_reason
  - gotchas: NEVER deleted — CANCELLED/NO_SHOW kept for analytics (line 4; migration 0012:26 'NO DELETE on appointments'). Two PII paths: (1) the customer_id FK (registered customers) and (2) PLAINTEXT walk-in contact fields contact_name/contact_phone/contact_email for source=WEB bookings (lines 57-59, migration 0062) — these are raw, unencrypted personal data and MUST be NULLed. customer_notes/staff_notes/cancellation_reason are free-text that can embed PII — NULL them too. All these columns are nullable, so NULLing is clean. App role HAS the UPDATE grant on (source, contact_name, contact_phone, contact_email) (migration 0062:75) so the anonymize UPDATE is permitted. GOTCHA: appointments has a BEFORE UPDATE transition-validator trigger (migration 0012:386) and a scheduling-field immutability check after check-in; a contact-field-only UPDATE keeps status unchanged so the transition validator early-returns (0012:398) and will not block it — but verify the immutability trigger does not also guard contact fields. For erasure of a customer's appointments, sweep BOTH where customer_id = subject AND any rows whose contact_* match the subject's walk-in bookings. ends_at is GENERATED ALWAYS (line 39) — never write it.
- **appointment_notifications** [null-pii] (packages/db/src/schema/appointments/appointmentNotifications.ts:29) — link: appointment_id (NOT NULL FK -> appointments.id, line 33) — links to subject indirectly via the appointment — PII: recipient, external_ref
  - gotchas: MAJOR PRIVILEGE GOTCHA. recipient (line 38) holds the raw plaintext phone OR email the reminder was sent to ('phone or email (or pos for sse)', migration 0038:22) — direct PII of the data subject — and is NOT NULL. The erasure job CANNOT clear it under the normal app/worker role: migration 0038 grants app/worker only UPDATE (sent_at, delivery_status, external_ref) and there is NO DELETE grant and NO UPDATE grant on `recipient` anywhere. So anonymizing this column requires a SECURITY DEFINER function or the table-owner role; and because recipient is NOT NULL you cannot NULL it — overwrite with a redaction sentinel (e.g. 'REDACTED'). external_ref (provider message id, line 43) is in the app/worker UPDATE grant and can be cleared normally. Reach these rows via appointment_id -> appointments of the subject. No fiscal value, but no-delete by grant, so anonymize-in-place is the only path.
- **product_viewing_holds** [no-pii] (packages/db/src/schema/appointments/productViewingHolds.ts:17) — link: customer_id (nullable FK -> customers.id, line 27); also appointment_id FK — PII: released_reason
  - gotchas: Operational hold rows, NEVER deleted (released via released_at + reason, line 6). Carries customer_id as a LINK but no encrypted PII of its own. released_reason is operator free-text (could theoretically carry a name) — low risk; the released_has_reason CHECK (line 49) couples released_at = released_reason as all-or-nothing, so do NOT NULL released_reason alone on a released row (it would violate the CHECK). Practically: leave released_reason, just rely on the customers-row anonymize to neutralize the customer_id link. The customer_id FK itself can stay pointing at the now-anonymized customer shell (the customers row is kept, so no FK-ordering problem). No action required beyond the customers-table anonymize.
- **appointment_linked_products** [no-pii] (packages/db/src/schema/appointments/appointmentLinkedProducts.ts:21) — link: none (links appointment_id <-> product_id only; reaches the subject only transitively via appointments.customer_id) — PII: none
  - gotchas: M:N join, INSERT-only by discipline (line 4). No customer link and no PII. No erasure action.
- **document_attachments** [keep-fiscal] (packages/db/src/schema/documents/documentAttachments.ts:31) — link: customer_id (nullable FK -> customers.id, line 43; polymorphic — exactly ONE of customer/product/transaction/appraisal) — PII: r2_key, file_name, notes, sha256_hex, category(AUSWEIS)
  - gotchas: Evidentiary attachments — soft-delete only via archived_at, NEVER hard-delete (line 17). The bytes live in R2 (the actual file must be deleted out-of-band from object storage; the DB only carries the key). For a customer's erasure target the rows where customer_id = subject. CRITICAL: category='AUSWEIS' rows ARE the customer's ID-document scans (PII) — but the AUSWEIS evidence overlaps the kyc_documents flow; treat as keep-fiscal: NULL/redact the PII-bearing metadata (file_name, notes, sha256_hex) and the r2_key (after deleting the R2 object), set archived_at, but keep the row. GOTCHA #1 — exactlyOneLink CHECK (line 91): you must NOT NULL customer_id, because exactly one of the four links must remain non-NULL; clearing customer_id would violate the CHECK (and the row would lose its anchor). Keep the link; the customers row is anonymized in place so the FK stays valid. GOTCHA #2 — category-specific CHECKs: ausweis_is_customer (line 100) REQUIRES customer_id NOT NULL for AUSWEIS, ankaufbeleg/rechnung require customer_id OR transaction_id — so for an ANKAUFBELEG/RECHNUNG linked only by customer_id you also cannot NULL the link. GOTCHA #3 — r2_key has a length 1..1024 CHECK (line 73) and file_name 1..255 (line 77), both NOT NULL — you cannot NULL them; overwrite with a short redaction sentinel instead. sha256_hex format CHECK (line 86) allows NULL, so sha256_hex can be NULLed. Fiscal-category docs (ANKAUFBELEG/RECHNUNG/VERSANDBELEG) are GoBD 10-year records — keep the row, redact embedded PII only.
- **whatsapp_inbound_messages** [null-pii] (packages/db/src/schema/whatsapp/index.ts:36) — link: linked_customer_id (uuid -> customers.id, nullable, operator-triage, migration 0031). NB: the real subject key is from_phone, not the FK — most rows are never linked. — PII: from_phone, raw_payload, body_encrypted, processing_error
  - gotchas: HARD BLOCKER: app role has NO UPDATE grant on from_phone/raw_payload/body_encrypted — migration 0019:519 + 0036:63 grant UPDATE only on (processed_at, processing_error, handled_at, handled_by_user_id, linked_customer_id, body_encrypted). So the erasure transaction CAN null body_encrypted but CANNOT null from_phone or raw_payload as the app role; needs an elevated/worker path or a new GRANT. raw_payload is the worst PII sink: the full Meta webhook JSON carries the sender phone AND the plaintext message text — nulling body_encrypted alone does NOT erase it. CHECK whatsapp_inbound_payload_object (jsonb_typeof(raw_payload)='object') BLOCKS setting raw_payload to NULL or to a scalar — must set it to '{}'::jsonb, not NULL. from_phone is NOT NULL (cannot null; overwrite with a redaction token). processing_error is free-text that may echo the phone. Subject match is by phone (from_phone), since linked_customer_id is usually NULL — erasure must resolve the customer's phone via customers + whatsapp_conversations and sweep all rows with that from_phone. unhandledIdx is on from_phone (non-unique; redacting it is fine). meta_message_id unique index is non-PII, leave it.
- **whatsapp_outbound_messages** [null-pii] (packages/db/src/schema/whatsapp/index.ts:92) — link: none (no customer FK; subject identified by to_phone). sent_by_user_id -> users.id is STAFF, not the data subject. — PII: to_phone, body, body_encrypted, template_params, provider_error
  - gotchas: HARD BLOCKER: app role has only SELECT+INSERT (migration 0031:148) plus UPDATE(body_encrypted) (0036:64) — it CANNOT UPDATE to_phone/body/template_params/provider_error. Erasure of these needs an elevated grant or worker. CHECK whatsapp_outbound_body_nonempty (length(body)>0) BLOCKS nulling/emptying body — must overwrite with a non-empty redaction sentinel (e.g. a single placeholder char), not '' or NULL; body is also NOT NULL. body is the plaintext reply (PII: free text the cashier/bot wrote, may name the customer). to_phone is NOT NULL — overwrite, don't null. template_params jsonb often holds the customer name/order details. provider_error jsonb may echo the recipient phone. CHECK whatsapp_outbound_error_status_check ties status='failed' to provider_error IS NOT NULL — do NOT null provider_error on a failed row or the CHECK breaks; redact its contents to a stripped object instead. Match subject by to_phone.
- **whatsapp_conversations** [null-pii] (packages/db/src/schema/whatsapp/index.ts:133) — link: customer_id (uuid -> customers.id, nullable). Primary subject key is customer_phone_e164. — PII: customer_phone_e164
  - gotchas: This is the designed anonymize-in-place target: schema doc + migration 0036:23 say 'anonymized_at stamped on an erase request', and app HAS GRANT UPDATE(customer_id, anonymized_at, ...) at 0036:60. BUT that grant list does NOT include customer_phone_e164 — so the app CANNOT null the phone itself, only stamp anonymized_at and clear customer_id. To actually scrub the phone you need an added grant or worker. customer_phone_e164 is NOT NULL AND has UNIQUE index whatsapp_conversations_phone_uq — cannot null it and cannot reuse a constant sentinel across two erased customers (second collides on the unique index); use a per-row unique redaction token (e.g. 'erased:'||id). Keep the row (it's the join key for the message sweep); set anonymized_at, null customer_id, redact the phone, and bump retention_until is optional. ai_active/cooldown carry no PII.
- **ai_calls** [no-pii] (packages/db/src/schema/whatsapp/index.ts:166) — link: conversation_id (uuid -> whatsapp_conversations.id, nullable). No direct customer link. — PII: none
  - gotchas: Cost-ledger only (kind/model/token counts/cost_eur/timestamps). No message content, no phone, no name. Keep rows as-is for AI cost accounting; the conversation_id FK can stay pointing at the now-anonymized conversation row (do NOT cascade-delete). App has no UPDATE/DELETE here anyway. No action needed.
- **audit_log** [keep-fiscal] (packages/db/src/schema/audit/auditLog.ts:25) — link: none structurally. actor_user_id -> users.id is STAFF (the operator who acted), NOT the customer. The customer/shopper appears ONLY inside payload jsonb (e.g. customerId, kycDocumentId — see emitAudit calls like customer.kyc_purged). — PII: ip_address, user_agent, payload
  - gotchas: Treat as immutable security evidence: migration 0008:310 + table comment say append-only — app role has SELECT+INSERT ONLY, NO UPDATE, NO DELETE. The ONLY UPDATE grant is the worker on ip_address (migration 0041:56) used by gdpr_cleanup (apps/worker/src/jobs/gdpr-cleanup.ts) which masks IPs after 180 days — that is the ONLY existing scrub. GAP for Art.17: customer references in payload jsonb are NOT cleaned by anything today, and no role can UPDATE payload. ip_address/user_agent of the ACTOR are staff PII, not the customer's — generally keep (security threat-model record), but they are masked at 180d. KEY DECISION: audit_log entries are non-fiscal but are security evidence; the warehouse14 convention already writes REDACTED payloads (UUIDs + reason, no PII — see gdpr-cleanup emitAudit at line 221). So the correct posture is keep-row, and rely on the existing no-raw-PII-in-payload discipline rather than mutating history. If a payload DID capture raw customer PII, scrubbing it would require a privileged migration/grant since the app cannot UPDATE. CHECK audit_log_payload_object forces payload to stay a jsonb object (cannot null it). Do NOT attempt app-role DELETE — it will fail (no grant).
- **ledger_events** [keep-fiscal] (packages/db/src/schema/audit/ledgerEvents.ts:37) — link: none structurally. actor_user_id -> users.id is STAFF. entity_table/entity_id is a generic pointer that CAN point at a customer row; customer data may also sit inside payload jsonb. — PII: ip_address, payload
  - gotchas: STRICTLY IMMUTABLE — this is the §259 StGB / GoBD tamper-evident hash-chain journal. Migration 0008:274 'NO GRANT UPDATE, NO GRANT DELETE on ledger_events. Ever.' App role has SELECT + column-restricted INSERT only. Mutating ANY column would break the SHA-256 prev_hash/row_hash chain (CHECK ledger_events_prev_hash_length / row_hash_length enforce 32-byte digests; verify_ledger_chain() would fail). Therefore: keep every row, NULL nothing — the legal immutability overrides Art.17 here (10-year retention wins). gdpr_cleanup deliberately leaves ledger_events.ip_address FULL (comment at job line 6). The ONLY mitigation is the upstream discipline that payloads carry references/UUIDs, not raw decryptable PII (PII lives encrypted in source tables). entity_id pointing at an anonymized customer is fine — the customer row survives anonymized-in-place, so the FK-by-value reference still resolves. Do not include this table in the erasure UPDATE/DELETE set.
- **accounts** [no-pii] (packages/db/src/schema/auth/accounts.ts:28) — link: none — user_id -> users.id is a STAFF user (dealer employee/owner), NOT a customer or storefront shopper. Confirmed: whole auth/* domain references only auth.users. — PII: password, access_token, refresh_token, id_token, account_id
  - gotchas: Out of scope for CUSTOMER/SHOPPER erasure — these are staff credentials. (Storefront shopper accounts live in the storefront domain, migration 0066_shopper_google_oauth, NOT here.) For completeness if a STAFF member is erased: comment says NEVER DELETE (audit evidence of past auth links); secrets would be scrubbed via the users soft-delete/anonymize flow, and CHECK accounts_credentials_or_oauth constrains password/access_token mutual exclusivity so any scrub must keep that shape. No customer PII — no action in this erasure.
- **sessions** [no-pii] (packages/db/src/schema/auth/sessions.ts:35) — link: none — user_id -> users.id is STAFF. No customer/shopper link. — PII: ip_address, user_agent, token
  - gotchas: Staff-only auth sessions; ip/user_agent are the EMPLOYEE's, not the customer's. App role HAS full grants incl DELETE here (schema doc line 5) — but irrelevant to customer erasure. Not part of the customer/shopper erasure surface.
- **users** [no-pii] (packages/db/src/schema/auth/users.ts:40) — link: n/a — this IS the staff identity table, not a customer/shopper. It is the canonical model the CUSTOMERS table soft-delete/anonymize pattern was copied from (softDeletedAt + anonymizedAt). — PII: email, name, image
  - gotchas: STAFF, not the data subject of customer/shopper erasure — exclude from this job. Documented here only as the reference anonymize-in-place pattern: NEVER deleted, GDPR via soft_deleted_at + anonymized_at, with CHECKs users_anonymized_implies_soft_deleted and users_anonymized_after_soft_deleted enforcing ordering, and partial-unique users_email_active_uq (WHERE soft_deleted_at IS NULL) that frees the email on soft-delete. If/when a staff member is erased that is a SEPARATE flow keyed on users.id, not the customer-erasure transaction.
- **devices** [no-pii] (packages/db/src/schema/auth/devices.ts:40) — link: none — paired_by_user_id -> users.id is STAFF. mTLS terminal records. — PII: last_seen_ip, hostname, notes
  - gotchas: Hardware/terminal identity, no customer PII. NEVER DELETE (comment). Not part of customer erasure.
- **two_factors** [no-pii] (packages/db/src/schema/auth/twoFactors.ts:22) — link: none — user_id -> users.id is STAFF. — PII: secret, backup_codes
  - gotchas: Encrypted staff TOTP secrets. App may DELETE (admin-mediated disable). Not customer data — exclude.
- **verifications** [no-pii] (packages/db/src/schema/auth/verifications.ts:16) — link: none — short-lived better-auth tokens, no FK. — PII: identifier
  - gotchas: identifier CAN be a staff email/phone for verification flows, but these are ephemeral (expires_at TTL, insert-and-consume, app has DELETE). Tied to STAFF auth, not customers/shoppers. Not part of customer erasure; natural expiry/delete handles it.
- **transactions** [keep-fiscal] (packages/db/src/schema/transactions/transactions.ts:42 (FK customer_id :51; shipping_address_encrypted :79; migration 0009_transactions.sql GRANT UPDATE :409, 0018_storefront_commerce.sql shipping cols/CHECK :354,372,447)) — link: customer_id (:51, nullable FK -> customers.id). NOTE: storefront shoppers reach this table THROUGH their 1:1 customers row — there is no shopper_id column; a WEB/eBay sale carries the shopper's underlying customer_id. — PII: shipping_address_encrypted, notes_internal
  - gotchas: 10-year GoBD immutable fiscal master — KEEP every row, NULL only embedded PII. HARD BLOCKER: shipping_address_encrypted has NO app UPDATE grant (0018 line 447: 'sales_channel + shipping_address_encrypted are SET AT INSERT ONLY — no UPDATE grant'); the only app-updatable cols are printed_at/receipt_locator/notes_internal/updated_at (0009:409) + shipping_status/shipping_carrier/tracking_number (0018). Erasure MUST run as a privileged/owner role (or a SECURITY DEFINER fn) to NULL shipping_address_encrypted, NOT the app role. notes_internal is free-text and app-updatable — scrub it too if it holds buyer PII. customer_id is NOT cleared (keeps the link to the now-anonymized customer; trigger trg_transactions_validate_storno in 0009:301 ENFORCES 'Storno customer must match the original' — clearing customer_id on one of a storno pair would break that invariant). No buyer name/contact column lives here; shipping_address_encrypted is the only address blob.
- **transaction_items** [keep-fiscal] (packages/db/src/schema/transactions/transactionItems.ts:26) — link: none (links to transactions.id only, :30) — PII: line_discount_reason
  - gotchas: INSERT-only immutable §25a margin snapshot — no app UPDATE/DELETE grant at all (0009 comment: 'app cannot UPDATE anything'). line_discount_reason is free-text and could in theory name a customer, but it is immutable and unreachable by the app role; treat as keep-fiscal, scrub only via privileged role if a reason string is found to embed PII. No customer FK.
- **transaction_payments** [keep-fiscal] (packages/db/src/schema/transactions/transactionPayments.ts:15) — link: none (links to transactions.id only, :19) — PII: none
  - gotchas: INSERT-only immutable payment evidence (no UPDATE grant). PCI-minimized: only masked last-4 (zvt_card_pan_masked, CHECK regex '^\*+\d{4}$' at :43) — masked PAN is not erasable PII and a CHECK would block writing a non-masked value anyway. external_ref/zvt_*/mollie_payment_id are processor tokens, not customer PII. No customer FK. keep as-is.
- **vouchers** [null-pii] (packages/db/src/schema/vouchers/index.ts:19 (FK issued_to_customer_id :30; notes :34; GRANT UPDATE migration 0019_retail_compliance.sql:503)) — link: issued_to_customer_id (:30, nullable FK -> customers.id) — the voucher holder — PII: notes
  - gotchas: Has VAT/fiscal weight (§3 Abs.14 UStG) so the ROW must survive (NEVER DELETE per 0019), but it is NOT in the immutable-trigger set — only a trg_vouchers_updated_at trigger exists, no column lock. notes IS app-updatable (0019:503 grants UPDATE on current_balance_eur,status,expires_at,notes,updated_at) → NULL notes to scrub any holder name/contact. GOTCHA: issued_to_customer_id is NOT in the UPDATE grant, so the app role CANNOT clear the FK — anonymizing the linked customers row is what de-identifies the holder; clearing the FK requires a privileged role and is not strictly needed. Holder name/contact is NOT stored inline — it lives only via the customer FK plus free-text notes.
- **voucher_redemptions** [no-pii] (packages/db/src/schema/vouchers/index.ts:58) — link: none (links voucher_id + transaction_id only, :62/:65) — PII: none
  - gotchas: Append-only redemption log (0019: 'voucher_redemptions: append-only' — no UPDATE/DELETE grant). No PII, no customer FK (reaches the holder only transitively via the voucher). Keep untouched.
- **daily_closings** [no-pii] (packages/db/src/schema/closing/dailyClosings.ts:41) — link: none (Z-report aggregates per business day; only user FKs counted_by/finalized_by) — PII: notes
  - gotchas: Immutable once FINALIZED (BEFORE UPDATE trigger locks every numeric/anchor/finalization marker; only `notes` stays editable). Aggregate fiscal data — no customer PII (vat_by_treatment/payments_by_method are jsonb aggregates keyed by treatment/method, not by customer). `notes` is the operator's Z-report note; scrub only if a subject's name was hand-typed there. No customer/shopper link. Treat as no-pii / keep-fiscal.
- **dsfinvk_exports** [no-pii] (packages/db/src/schema/closing/dsfinvkExports.ts:34) — link: none (period-level DSFinV-K bundle audit trail; only requested_by_user_id FK) — PII: none
  - gotchas: NEVER deleted by app role. delivery_target is the Steuerberater's address/email (a B2B processor target, NOT the data subject) — leave it. The generated R2 bundle (r2_key) CONTAINS transaction-level data incl. any shipping address, but that is an exported artifact, out of DB scope; flag to the erasure owner that already-generated DSFinV-K exports in R2 may embed the subject's shipping address and cannot be retroactively scrubbed (legitimate GoBD retention overrides Art.17 here). Row itself: no in-table customer PII.
- **system_settings** [no-pii] (packages/db/src/schema/closing/systemSettings.ts:17) — link: none (key/value config store) — PII: none
  - gotchas: Config KV store, NEVER deleted by app role; no customer/shopper data. Out of erasure scope.
- **fixed_costs** [no-pii] (packages/db/src/schema/finance/fixedCosts.ts:23) — link: none (Owner-facing Fixkosten planning table) — PII: none
  - gotchas: Business cost lines (Miete/Strom), no customer/shopper data. NEVER hard-delete (close with active_to). Out of subject-erasure scope.
- **operating_expenses** [no-pii] (packages/db/src/schema/finance/operatingExpenses.ts:24) — link: none (Betriebsausgaben booked per business day; only created_by_user_id FK) — PII: note
  - gotchas: Business expense ledger, GoBD-unveränderbar (NEVER hard-delete; corrections are new rows). `note` is a staff-typed expense memo (note_length CHECK <=500) — not customer data by design; scrub only if a subject name was hand-typed. No customer/shopper link. Treat as no-pii.
- **tse_signatures** [keep-fiscal] (packages/db/src/schema/tse/tseSignatures.ts:36 (immutability migration 0054_tse_signature_persistence.sql)) — link: none (one row per transaction via transaction_id :41; device_id/recorded_by_user_id are not customer) — PII: none
  - gotchas: STRICTLY IMMUTABLE — app role is INSERT+SELECT only; a BEFORE UPDATE/DELETE trigger HARD-REFUSES any mutation (BSI TR-03153 / GoBD signature evidence). Cryptographic signature material (signature_value/qr_code_data/cert) is NOT personal data of the customer. Do NOT touch — any erasure UPDATE here will be rejected by the trigger. Reaches the subject only transitively via transactions.transaction_id.
- **tse_transactions** [keep-fiscal] (packages/db/src/schema/tse/tseTransactions.ts:41 (immutability migration 0010_tse.sql)) — link: none (one row per transaction via transaction_id :46) — PII: none
  - gotchas: NEVER deleted by app role; trigger enforces state-transition validity + signature immutability after FINISHED. Fiskaly state machine + signature evidence — no customer PII (process_data_hash is a hash, not data). Keep untouched; reaches subject only via transaction_id.
- **tse_signatures (FK note) / tse_daily_archives** [no-pii] (packages/db/src/schema/tse/tseDailyArchives.ts:20 (migration 0040_tse_daily_archives.sql)) — link: none (one archive per calendar day) — PII: none
  - gotchas: KassenSichV §10 daily archive evidence, append-only in spirit. No customer link or PII in-row (sha256/r2_key are over the day's TSE bundle). Like DSFinV-K, the archived R2 TAR may transitively contain transaction data but the DB row holds none. Keep.
- **tse_clients** [no-pii] (packages/db/src/schema/tse/tseClients.ts:18 (migration 0043_tse_clients.sql)) — link: none (one row per Fiskaly TSS) — PII: none
  - gotchas: TSS certificate-expiry tracking, no customer data. Out of erasure scope.
- **appraisals** [null-pii] (packages/db/src/schema/appraisals/index.ts:39-100) — link: customer_id (NOT NULL FK -> customers.id, line 43-45) — PII: notes, rejection_reason
  - gotchas: customer_id is NOT NULL so the row cannot be orphaned/deleted — keep the row, NULL the free-text. GOTCHA: CHECK appraisals_rejected_has_reason (line 95-98) forces rejection_reason IS NOT NULL whenever status='REJECTED'; you CANNOT simply NULL rejection_reason on a rejected appraisal — overwrite with a non-PII placeholder (e.g. 'GELOESCHT') instead of NULL. If status='ACCEPTED' the appraisal is tied to ankauf_transaction_id (fiscal) via the appraisals_accepted_has_evidence CHECK — do not delete; treat as keep-fiscal-with-anonymize. notes is always nullable.
- **appraisal_items** [null-pii] (packages/db/src/schema/appraisals/index.ts:105-143) — link: none (links to appraisals.id; reach the seller only via parent appraisals.customer_id) — PII: notes, description, photo_r2_keys
  - gotchas: No direct customer FK — must be reached through the parent appraisal. description/notes are free-text valuation notes that can name or describe the seller; photo_r2_keys is a text[] of R2 object keys for photos of the seller's goods (item imagery, possible PII/biometric) — NULL the array (default '{}') AND schedule the underlying R2 objects for deletion (bytes live outside Postgres). name is NOT NULL (line 114) but is an item label, not personal data — leave it. No CHECK blocks NULLing these.
- **staff_phone_numbers** [no-pii] (packages/db/src/schema/intake/index.ts:55-83) — link: none (user_id -> users.id; this is STAFF, not a customer/shopper) — PII: none
  - gotchas: This is the dealer's own staff field-buyer phone identity (E.164), NOT a data-subject customer/shopper. Out of scope for customer erasure. phone_e164 is employee PII governed by employment law, not Art.17 customer erasure. Do not touch in a customer-erasure transaction.
- **intake_sessions** [null-pii] (packages/db/src/schema/intake/index.ts:88-113) — link: none (keys on staff_phone_id; NO customer/shopper FK exists anywhere in intake) — PII: rejected_reason
  - gotchas: ORPHAN-PII RISK: intake is the pre-customer staging pipeline — a seller's data is captured here BEFORE any customers row exists, so there is NO customer_id to join on. A customer-id-driven erasure will MISS these rows entirely. rejected_reason is free-text that may quote the seller. Erasure for intake must be driven by session/phone correlation (or a retention sweep), not the customer FK. product_id (line 98) only links to inventory, not a person.
- **intake_messages** [null-pii] (packages/db/src/schema/intake/index.ts:121-144) — link: none (session_id -> intake_sessions.id; no customer link) — PII: text_body, media_r2_key, whatsapp_message_id
  - gotchas: HIGHEST raw-PII concentration in my domains: text_body is the literal inbound WhatsApp message body from the seller (name/address/price negotiation free-text); media_r2_key points at seller-supplied photos in R2; whatsapp_message_id (wamid) is a WhatsApp/Meta identifier tied to the seller's phone. NULL text_body + media_r2_key (both nullable, lines 71-72) and delete the R2 objects. GOTCHA: whatsapp_message_id is NOT NULL + UNIQUE (intake_messages_wamid_uq, line 137) — you cannot NULL it; if it must be cleared, overwrite with a unique non-PII token (e.g. 'erased:'||id) to preserve the unique-idempotency index. Module header says 'NEVER deleted' so keep rows, anonymize in place. No customer FK — orphan-PII, must be reached via session, not customer-id.
- **intake_drafts** [null-pii] (packages/db/src/schema/intake/index.ts:151-182) — link: none (session_id -> intake_sessions.id, UNIQUE; no customer link) — PII: bg_removed_photo_keys, vision_classification, vision_hallmark_detection, vision_scale_reading, classifier_explanation, german_description, marketing_angles, embedding, final_data
  - gotchas: AI-derived seller data with NO customer FK (orphan-PII, reach via session only). final_data + the vision_* JSONB + german_description/classifier_explanation are model outputs over the seller's photos/messages and can echo personal data; bg_removed_photo_keys is a text[] of R2 keys for the seller's item photos (delete the R2 bytes too); embedding (vector 1536) is derived from that content and should also be cleared. All columns are nullable — straight NULL works, no CHECK blocks it. session_id is UNIQUE NOT NULL — keep the row (header: 'NEVER deleted'), null the payload.
- **inventory_sessions** [no-pii] (packages/db/src/schema/inventory/index.ts:30-64) — link: none (opened_by/closed_by -> users.id only) — PII: none
  - gotchas: Stocktake session — only staff user FKs and counts. notes (line 45) is operational stocktake commentary, not customer data; treat as no-PII unless a sweep finds a seller name was typed there (unlikely). No customer/shopper link. closed_has_evidence CHECK exists but is irrelevant to erasure.
- **inventory_scans** [no-pii] (packages/db/src/schema/inventory/index.ts:69-90) — link: none (scanned_by -> users.id; product_id -> products.id) — PII: none
  - gotchas: Barcode scan log — raw_barcode is a product/SKU barcode, not personal data. No customer link. Nothing to erase.
- **products** [null-pii] (packages/db/src/schema/products/products.ts:57-292) — link: acquired_from_customer_id (nullable FK -> customers.id, line 118) — PII: provenance_notes
  - gotchas: Inventory authority — header says NEVER DELETE (full audit trail) and most columns are intake-locked/fiscal (acquisition_cost_eur is §25a-immutable). So keep every row: (1) NULL provenance_notes (line 165, nullable free-text that can name the seller); (2) NULL acquired_from_customer_id to sever the link to the erased customer (nullable FK, no CHECK requires it). Do NOT touch sku/barcode/classification/acquisition_cost (intake-locked + fiscal). Note app-role UPDATE grant excludes intake-locked cols (migration 0006 §4) — the erasure must run as the privileged role, not warehouse14_app, or the UPDATE on provenance_notes/acquired_from_customer_id must be confirmed inside the app grant set. description_de/seo_* are product copy, not PII.
- **product_photos** [no-pii] (packages/db/src/schema/products/productPhotos.ts:43-130) — link: none (product_id -> products.id; workflow user FK only) — PII: none
  - gotchas: Photos are of the inventory item, not the person, and are reached via product not customer. App role HAS DELETE here (header) but there is no customer linkage to drive erasure. Out of scope for customer erasure (item imagery, not personal data). Any seller-identifying item photo is handled upstream at appraisal_items.photo_r2_keys / intake media, not here.
- **vouchers** [null-pii] (packages/db/src/schema/vouchers/index.ts:19-53) — link: issued_to_customer_id (nullable FK -> customers.id, line 30) — PII: notes
  - gotchas: Voucher is a §3(14) UStG fiscal instrument but the master row itself is not the immutable ledger — keep the row and (1) NULL notes (free-text, may name the recipient), (2) NULL issued_to_customer_id to sever the customer link (nullable FK, no CHECK requires it; partial index vouchers_customer_idx is WHERE issued_to_customer_id IS NOT NULL so NULLing just drops it from the index — safe). Do NOT delete: balance/issuance feeds VAT. code/issued_value/tax_treatment are fiscal — leave them. issued_by_transaction_id keeps the fiscal trail intact.
- **voucher_redemptions** [keep-fiscal] (packages/db/src/schema/vouchers/index.ts:58-75) — link: none (voucher_id + transaction_id only) — PII: none
  - gotchas: Immutable redemption ledger (VAT-at-redemption record, 10-year GoBD). No PII columns and no direct customer link — reachable only via voucher_id/transaction_id. Keep every row untouched; nothing to NULL. Severing happens upstream at vouchers.issued_to_customer_id and on the transaction, not here.
- **shoppers** [null-pii] (packages/db/src/schema/storefront/shoppers.ts:33) — link: customer_id (UUID, NOT NULL, UNIQUE, FK -> customers.id; 1:1 with the KYC/customers row) — PII: email_encrypted, email_blind_index, phone_encrypted, phone_blind_index, shipping_recipient_name_encrypted, shipping_address_line1_encrypted, shipping_address_line2_encrypted, shipping_postal_code_encrypted, shipping_city_encrypted, shipping_country, billing_recipient_name_encrypted, billing_address_line1_encrypted, billing_address_line2_encrypted, billing_postal_code_encrypted, billing_city_encrypted, billing_country, password_hash, google_sub, email_verification_token, marketing_consent, marketing_consent_at, preferred_language
  - gotchas: THE storefront data-subject. Mirrors the customers ANONYMIZE-IN-PLACE discipline exactly: soft_deleted_at + anonymized_at (NEVER row-delete; table comment + COMMENT ON TABLE 0018:165-167 say so; no DELETE grant on shoppers anywhere). HARD BLOCKER: the GRANT UPDATE list (migration 0018:402-417 + 0066:37) does NOT include email_encrypted or email_blind_index. So the app role can NULL/scramble every other PII column but CANNOT touch the encrypted email or its blind index — the anonymize transaction must run under an elevated role (warehouse14_security/owner) or 0018 must be amended to grant UPDATE on those two columns. CHECK shoppers_anonymized_implies_soft_deleted (line 102) forces order: set soft_deleted_at BEFORE/with anonymized_at, never anonymized alone. CHECK shoppers_has_credential (line 118) requires password_hash IS NOT NULL OR google_sub IS NOT NULL — you CANNOT NULL both credentials; to neutralize login either keep one as a dead value or scramble password_hash to a random non-null hash while NULLing google_sub. CHECK shoppers_marketing_consent_has_timestamp (106): if you keep marketing_consent=TRUE you must keep a timestamp; safest is set marketing_consent=FALSE (then consent_at may be NULL). CHECK shoppers_country_iso2_* (94/98): country cols are nullable so NULL is fine. Partial UNIQUE shoppers_email_blind_active_uq WHERE soft_deleted_at IS NULL (line 83) — setting soft_deleted_at releases the email from the active-unique constraint (re-signup allowed) WITHOUT needing to clear the blind index; this is why row-keep works. Partial UNIQUE shoppers_google_sub_active_uq likewise keyed on soft_deleted_at IS NULL. NOT NULL on email_encrypted/email_blind_index means you cannot NULL them anyway — overwrite with an encrypted tombstone constant instead.
- **shopper_sessions** [delete-row] (packages/db/src/schema/storefront/shopperSessions.ts:13) — link: shopper_id (UUID, NOT NULL, FK -> shoppers.id) — PII: ip_address, user_agent, token
  - gotchas: Auth sessions, no fiscal value. ip_address (inet) + user_agent are personal data; token is a live credential. App role HAS GRANT DELETE (0018:420) and worker has DELETE too (0018:456, expired-session sweeper). Just DELETE all rows for the shopper_id as part of erasure (also kills any live login). FK ordering: delete these before/independently of the shoppers row (but shoppers row is kept anyway, so no ordering conflict).
- **carts** [keep-fiscal] (packages/db/src/schema/storefront/carts.ts:25) — link: shopper_id (UUID, NOT NULL, FK -> shoppers.id) — PII: none
  - gotchas: No PII columns of its own (shopper_id is a UUID link, not PII). BUT a CONVERTED cart links to a fiscal sale: converted_to_transaction_id (UNIQUE, FK -> transactions.id) and CHECK carts_converted_has_transaction (line 60). A CONVERTED/CONVERTED-linked cart is the shopper-side of an immutable transaction — do NOT delete it (would orphan/contradict the fiscal record). Non-converted carts (ACTIVE/ABANDONED/CHECKOUT/RESERVED) have no fiscal value and could be delete-row, but the row itself carries no PII so the simplest correct action is keep-and-leave (shopper_id can stay since the shopper row is anonymized-in-place, not deleted). App role has NO DELETE grant on carts (only UPDATE status fields, 0018:423). NOTE (cross-domain, flag only): trigger trg_carts_after_reserve / on_cart_reserved (migration 0068) writes shopper_id (UUID only, no name/addr) into ledger_events.payload in the finance/audit domain — not PII, but the audit-domain owner should confirm.
- **cart_items** [no-pii] (packages/db/src/schema/storefront/carts.ts:67) — link: none (cart_id -> carts.id; reaches the shopper only transitively) — PII: none
  - gotchas: Product/price/quantity only. Reachable from a shopper via cart_id->carts.shopper_id but holds no personal data. App role has GRANT DELETE (0018:431) for remove-from-cart UX, so non-converted basket lines could be swept, but nothing requires it for erasure.
- **payment_intents** [keep-fiscal] (packages/db/src/schema/storefront/paymentIntents.ts:24) — link: none (cart_id, UNIQUE, FK -> carts.id; shopper only transitive) — PII: outcome (jsonb), client_secret, redirect_url, provider_intent_id
  - gotchas: Payment-provider record tied to a sale/cart. The jsonb `outcome` blob can contain provider-returned PII (cardholder/payer name, billing address, email from Stripe/PayPal/Mollie) and client_secret/redirect_url can embed identifiers — REVIEW and scrub outcome of any embedded personal data on erasure. Keep the row (payment evidence, ties to the immutable transaction via the cart). App role can UPDATE outcome/client_secret/redirect_url (0018:434-438) so scrubbing in place is permitted; CHECK payment_intents_outcome_is_object (line 49) requires outcome stays a JSON object — replace with '{}'::jsonb, do not NULL it. No DELETE grant.
- **webhook_events** [keep-fiscal] (packages/db/src/schema/storefront/webhookEvents.ts:21) — link: none (no shopper/customer FK; correlate only via payload contents) — PII: raw_body (text), payload (jsonb)
  - gotchas: Raw provider webhook bodies — raw_body (NOT NULL text) + payload (NOT NULL jsonb) frequently contain shopper email, name, billing/shipping address, payer details. There is NO shopper_id column, so an erasure job cannot target a subject's rows by FK — it must content-match (e.g. provider_event_id / payment intent id correlation) which is fragile. App role GRANT UPDATE is limited to (processed_at, processing_error) ONLY (0018:441) — it CANNOT rewrite raw_body or payload, and no DELETE grant. So scrubbing PII here requires an elevated role AND a correlation strategy; flag as a real erasure gap. raw_body is NOT NULL and payload CHECK requires jsonb object — overwrite with redacted constant / '{}', do not NULL.
- **internal_tasks** [null-pii] (packages/db/src/schema/tasks/internalTasks.ts:26) — link: related_entity_id + related_entity_table (polymorphic; related_entity_table may equal 'customers' per the whitelist CHECK, internal_tasks_related_entity_known, line 109). assigned_to_user_id / created_by_user_id are STAFF users, NOT customers. — PII: title, description, cancellation_reason
  - gotchas: Operator day-list. title/description/cancellation_reason are free text an operator could type a customer's name/phone into; a task can also point AT a customer via related_entity_table='customers' + related_entity_id. NEVER DELETE (comment line 16: forensic + GoBD-relevant) — keep the row. Erasure: when related_entity_table='customers' and related_entity_id = the erased customer, clear the polymorphic pointer AND review/redact the free-text fields. GOTCHA CHECK internal_tasks_related_entity_both_or_none (line 105): related_entity_table and related_entity_id must be BOTH NULL or BOTH set — null them together, never one. title is NOT NULL with CHECK length 1..200 (line 76) so cannot be NULLed — overwrite with a redaction placeholder. cancellation_reason NOT NULL+>=4 chars only when status=CANCELLED (CHECK line 89) — if redacting it on a cancelled task keep >=4 chars. assigned_to/created_by are user FKs (staff), out of customer-erasure scope.
- **business_locations** [no-pii] (packages/db/src/schema/locations/businessLocations.ts:19) — link: none — PII: none
  - gotchas: Holds name/street/postal/city/phone/email/lat/lng — but this is the SHOP's OWN address and contact, controller data, NOT a customer/shopper. Out of Art.17 data-subject scope. (Soft-deactivate via active=FALSE, never deleted — unrelated to GDPR.)
- **shifts** [no-pii] (packages/db/src/schema/shifts/index.ts:31) — link: none (device_id, opened_by_user_id, closed_by_user_id are devices/STAFF users) — PII: notes
  - gotchas: Cash-session reconciliation, fiscal/GoBD. Only staff/device FKs, no customer/shopper link. `notes` is free text but operator-facing cash-drawer notes, not customer PII; no customer linkage exists to target. Treat as no-PII for customer/shopper erasure (staff-employee erasure would be a separate exercise in the auth domain).
- **cash_movements** [no-pii] (packages/db/src/schema/shifts/index.ts:77) — link: none (shift_id; witness/performed_by are STAFF users) — PII: reason, external_ref
  - gotchas: Cash-drawer movement ledger (fiscal). reason (NOT NULL) + external_ref are free text but bank-drop/safe-transit notes, not customer data; no customer/shopper FK. No customer-erasure action.
- **belegtext_templates** [no-pii] (packages/db/src/schema/belegtext/belegtextTemplates.ts:23) — link: none (created_by_user_id is a STAFF user) — PII: none
  - gotchas: Versioned legal receipt/invoice boilerplate text. body_text/notes are generic legal copy, NOT customer-specific. No customer/shopper linkage. NEVER DELETE (Finanzamt audit) but irrelevant to customer erasure.
- **mcp_tool_invocations** [null-pii] (packages/db/src/schema/mcp/mcpToolInvocations.ts:43) — link: affected_entity_id + affected_entity_table (polymorphic; may reference customers/shoppers). actor_user_id is a STAFF user. — PII: arguments (jsonb), result (jsonb), error_message
  - gotchas: Append-only AI-action audit log. arguments + result jsonb can contain whatever was passed to/from an AI tool — potentially a customer's name/phone/address/email — and affected_entity_table/affected_entity_id can point at a customers or shoppers row. Keep the row (forensic/audit) but on erasure scrub jsonb of embedded subject PII and, where affected_entity points at the erased subject, consider redacting. GOTCHA CHECK mcp_tool_invocations_outcome_result_check (line 89): for outcome=SUCCESS, result MUST be NOT NULL (and error_code NULL); so you cannot NULL result on a SUCCESS row — overwrite with a redacted JSON object instead. arguments has NOT NULL + default '{}' — replace with '{}'::jsonb, do not NULL. No app-role UPDATE/DELETE grant in this migration (append-only) — scrubbing needs an elevated role.
- **worker_job_runs** [no-pii] (packages/db/src/schema/worker/workerJobRuns.ts:21) — link: none — PII: payload (jsonb), error_message
  - gotchas: Background-job run history. payload + error_message are operational job context (job name, counts, stack traces). No customer/shopper FK or identifier by design; not a per-subject store. Treat as no-PII for customer erasure (theoretical risk only if a job error message echoed a subject value — low, and there is no link to target it by).
- **worker_job_dlq** [no-pii] (packages/db/src/schema/worker/workerJobDlq.ts:25) — link: none (acked_by_user_id is a STAFF user; last_run_id -> worker_job_runs) — PII: payload (jsonb), last_error, ack_note
  - gotchas: Dead-letter queue. Same as worker_job_runs — operational payload/error/ack-note, no customer/shopper linkage to target. No customer-erasure action.
- **metal_prices** [no-pii] (packages/db/src/schema/metals/metalPrices.ts:32) — link: none (manual_override_by_user_id is a STAFF user; fetched_by_job_run_id -> worker_job_runs) — PII: none
  - gotchas: Edelmetallkurs price history. Only staff-user + job FKs and price data. NEVER DELETE (forensic/DSFinV-K) but no customer/shopper PII.
- **categories** [no-pii] (packages/db/src/schema/categories/categories.ts:29) — link: none — PII: none
  - gotchas: Product taxonomy. No personal data, no customer/shopper link.
- **product_categories** [no-pii] (packages/db/src/schema/categories/productCategories.ts:28) — link: none — PII: none
  - gotchas: Product<->category join. No personal data, no customer/shopper link.
- **hallmarks** [no-pii] (packages/db/src/schema/reference/hallmarks.ts:32) — link: none — PII: none
  - gotchas: Read-only reference lookup (stamp -> metal/fineness). No personal data.
- **karat_grades** [no-pii] (packages/db/src/schema/reference/karatGrades.ts:28) — link: none — PII: none
  - gotchas: Read-only reference lookup (karat -> fineness). No personal data.
- **tax_treatment_codes** [no-pii] (packages/db/src/schema/reference/taxTreatmentCodes.ts:20) — link: none — PII: none
  - gotchas: Read-only German tax-treatment reference. No personal data.
