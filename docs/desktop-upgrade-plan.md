# Warehouse14 Desktop — Definitive Upgrade Plan

## State of the desktop (honest assessment)

The desktop is **not a prototype — it is a strong, mostly production-grade cashier with a thin governance shell bolted beside it**, and the distance to the goal is real but bounded. The `tauri-pos` money engine (bigint-cents, banker's rounding, per-line VAT, TSE sandwich on Verkauf, hardened checkout mutex) and the Rust command surface (keychain-held secrets, AES-256-GCM KYC vault, fidelity-first mocks, minisign updater) are genuinely mature. But three classes of problem stand between today and "complete cashier + ideal governance + working OTA":

1. **Live-till correctness/fiscal holes** — a silently-dropped mixed-tax-treatment guard (`cart-store.ts` `addLine` only returns `ALREADY_IN_CART`), Ankauf with no TSE signing, a TSE offline queue that is never drained and is wiped on sign-out (`AppShell.tsx` `PER_OPERATOR_STORAGE_KEYS` includes `warehouse14.tse-queue.v1`), `pos_intents` crash-recovery defined but written by **zero** call sites, and two fiscal-grade Rust bugs — `zvt_reverse_payment` (`commands/zvt.rs:211`) parses a Storno with a naive single `buf[6]/buf[7]` read instead of the authorize path's full conversation loop, and the `MAX_BCD_CENTS`/zero-amount guard exists only in the mock (`mock/zvt_mock.rs:34`), so the real authorize builds an over-large frame that is green in mock and malformed on the wire.
2. **Doctrine drift the mobile app already eliminated** — **89** raw `err.message`/`error.message`/`j.error.message` leaks across the two desktop apps (verified count; the audit's "74" undercounts), no shared German-text spine, raw enum/UUID tokens on the compliance panel, a wrong channel-flags model, and zero read-side offline resilience.
3. **An unfinished shipping story** — control-desktop has **no login front door at all** (`control-desktop/main.tsx` mounts `<App/>` session-less), the active release workflow (`.github/workflows/release.yml`) ships POS only with **zero** `control` references and no `VITE_API_BASE_URL`, macOS is ad-hoc-signed (`signingIdentity: "-"`), Windows is unsigned, versions diverge hard (tauri-pos `tauri.conf.json` **0.4.33** vs `package.json` **0.1.0** — stale by 40+ releases, and the updater compares the Tauri version), `plugin-updater` drifts (POS 2.10.1 vs control 2.5.1), and the env build trap is reproduced verbatim (`main.tsx:38` falls back to `localhost:3001`, `.env.local` present on the build host).

None of this blocks the till from selling today, but several items are compliance exposure on a live regulated shop.

### Top 5 risks

1. **Fiscal-record loss is live, not theoretical.** The TSE offline queue (`tse-service.ts`) is never drained and its localStorage key is nuked on sign-out; Ankauf is never TSE-signed at all; `pos_intents` crash recovery is unimplemented. A Fiskaly outage plus a sign-out permanently destroys KassenSichV evidence.
2. **The env build trap will ship a dead till.** `main.tsx:38` falls back to `localhost:3001`, `.env.local` exists on the documented build host, and the active workflow does not force the prod URL — a `tauri build` bakes a dead API into the `.dmg`/`.exe`, silently defeating honesty doctrine (b).
3. **The governance surface has no front door.** `control-desktop/main.tsx` mounts session-less, so every ADMIN read 401s on launch and there is no login screen — the most compliance-critical app is unusable cold.
4. **OTA is half-wired and unsigned.** The active workflow builds POS only, control's `latest-control.json` never exists, macOS is ad-hoc-signed and Windows unsigned — so "one big update" trips Gatekeeper/SmartScreen and control can never self-update.
5. **Doctrine (a) is broken on the most-watched screens.** Raw machine tokens reach humans on `KonformitaetPanel`, Aufgaben priority, the appraisal metal hint, and ~89 `err.message` sites — the exact class of leak mobile spent ~200 commits eliminating.

### How this revision differs from the draft

This version absorbs the skeptical review in full: it splits `audit-ui.ts` into a portable vocabulary layer plus a per-platform presentation layer (the draft's 0.1 would not have typechecked); pulls the CSP-`localhost` drop and control-desktop login **forward** and pushes the `plugin-updater` bump **back** to the OTA phase where it can actually be tested; clusters all **signed fiscal-record** work into Phase 1 and adds the two missing Rust fiscal ports (ZVT reversal loop, ZVT amount guard); makes the grep-gate count the **exact regex set the sweep fixes** rather than a hand-counted number; splits the three mega-steps (`1.3`, `1.4`, `5.7`); promotes four-eyes strong-auth to a named sub-step; adds an **erasure-completeness audit** covering the temp-PDF and new read-cache PII sinks; and hardens the OTA phase (canonical-version required check, prerelease audit of existing tags, hardened-runtime entitlements, shared-minisign-key verify).

---

## Phase 0 — Safety net & shared foundation (no behavior change)

**Goal:** establish the tooling, shared package, and CI gates that every later phase depends on, without touching a single live-till behavior. Everything here is additive and reversible.

**Exit criteria:** the shared German-text package exists and is imported (nothing rendered changes yet); CI runs `pnpm --filter @norns/db build && pnpm -r typecheck` on every PR plus a desktop `cargo check` + `tauri build --no-bundle` for both apps; a grep-gate scaffold exists (warn-only) counting the exact regex set Phase 2 will drive to zero.

- **0.1 — Extract the portable German-text spine.** Lift `apps/mobile/src/warehouse14/german-text.ts` into a new platform-neutral package `@norns/i18n-de` exporting `describeError`, `describeConflict`, `describeUnauthorized`, `germanLabel()`, `CONFLICT_TOKENS`, `TASK_PRIORITY_LABEL`, and every `Record<Enum,string>` registry. Verified: `german-text.ts` imports **only** `@norns/api-client` symbols, so it lifts clean. **Doctrine:** (a). **Verify:** `pnpm -r typecheck` green; mobile still imports the same symbols (no behavior change); a unit test asserts an unknown enum degrades to `Unbekannt`, never the raw token.  ⟵ Quelle inzwischen geloescht, Portierung war abgeschlossen
- **0.2 — Split `audit-ui.ts` into vocabulary + presentation.** `audit-ui.ts` is **not** platform-neutral — verified it imports `lucide-react-native` and mobile `BadgeProps` (`apps/mobile/src/warehouse14/audit-ui.ts:36`) plus `kasse-ui`. Extract the **pure data** (`eventLabel`, `eventCategory`, `eventDomain`, `eventAction`, `DOCUMENT_CATEGORY_LABELS` re-exports, `CLOSING_STATE_LABELS`) into `@norns/i18n-de/audit-vocab.ts`; leave the **presentation** (icon components, badge variants) as a per-platform layer — mobile keeps its RN version, desktop gets its own icons/badges built in Phase 5. Repoint mobile's `audit-ui.ts` to import vocabulary from the package. **Doctrine:** (a), (c). **Verify:** `pnpm -r typecheck` green (the package has zero React-Native imports); mobile renders unchanged; Phases 5.2 and 6 depend on this split existing.  ⟵ Quelle inzwischen geloescht, Portierung war abgeschlossen
- **0.3 — Add the desktop build-validation CI job.** In `.github/workflows/ci.yml`, add a job that runs `cargo check` + `tauri build --no-bundle` for both `apps/tauri-pos/src-tauri` and `apps/control-desktop/src-tauri` on PRs touching `src-tauri` or shared packages. **Doctrine:** (c), (d). **Verify:** the job passes on a no-op PR; deliberately break a Rust file and confirm it fails.
- **0.4 — Add the honesty grep-gate with a frozen regex set (warn-only first).** Define one canonical regex set — `err\.message`, `error\.message`, `j\.error\.message`, `\?\? err`, `\?\?\s*.*status`, `humanizeEnum`, `localhost:3001` — in a committed `scripts/honesty-gate.sh`. The gate greps operator-facing code (excluding tests/`.d.ts`) and prints the count. Baseline is **whatever this exact set returns** (currently ~89), not a hand-typed number. **Doctrine:** (a), (b). **Verify:** the gate emits the live count; Phase 2's exit criterion is "this same script returns zero," so the sweep and the gate can never disagree.
- **0.5 — Reconcile the version scheme (tauri.conf canonical).** Pick `tauri.conf.json` as the single source of truth (the updater compares the Tauri version). Add a `scripts/set-version.mjs` that writes `package.json` + `Cargo.toml` + `tauri.conf.json` from the pushed tag for **both** apps, and a CI guard that **fails** when `tag != tauri.conf.version`. Note the real drift is large — tauri-pos npm is 0.1.0 while conf is 0.4.33 — so the first run of this script is a correction, done in this phase (no behavior change: the npm version is not shipped). **Doctrine:** (d). **Verify:** the script sets all three consistently for both apps; the CI guard fails on a mismatched tag. This guard becomes a **required status check** on the release workflow in Phase 8.
- **0.6 — Drop `localhost:3001` from the production CSP now.** Remove `http://localhost:3001` from the production CSP allow-list in `tauri.conf.json` for both apps (this is the same honesty defect as the env trap and must land **before** any OTA, not in a late security phase). Keep a dev-only CSP profile if needed. **Doctrine:** (b), security. **Verify:** grep confirms no `localhost` in the production CSP; the app still connects to `api.warehouse14.de` on the sim.

> **Deferred out of Phase 0:** the `plugin-updater` bump (POS 2.10.1 vs control 2.5.1) is a **behavior change to the auto-update path** and does not belong in a "changes nothing" phase — it moves to Phase 8 next to the OTA rehearsal where it can be tested end-to-end.

---

## Phase 1 — Fiscal & money-path correctness, including the full signed record (P0)

**Goal:** close every fiscal correctness/loss hole so the till is safe to run through a Fiskaly outage, a sign-out, and a crash — **and** make the signed fiscal record complete (VAT breakdown + process-data + correct ZVT framing) in one adjacent cluster, because these change the **signed** bytes and must not ship half-done. Highest-severity phase; lands before any release.

**Exit criteria:** a mixed-treatment cart is rejected with a German toast; Ankauf produces a `tse_signatures` row; the TSE offline queue drains and survives sign-out; `pos_intents` is written before every fiscal network call and reconciled on startup; the signed TSE FINISH body carries the real per-VAT decomposition and process-data; ZVT reversal parses via the full loop and authorize rejects oversized amounts before framing.

- **1.1 — Restore the mixed-tax-treatment guard.** In `apps/tauri-pos/src/state/cart-store.ts` re-add the same-`taxTreatmentCode` check in `addLine` returning `MIXED_TAX_TREATMENT`; handle that tag in `Verkauf.tsx` with a clear German toast and release the just-made hold. **Doctrine:** correctness/fiscal. **Verify:** a `cart-store` test asserts a `§25a` + `STANDARD_19` add is rejected; live-check on the sim that the toast fires and the header treatment stays consistent.
- **1.2 — Bring Ankauf under the TSE sandwich.** Wrap `AnkaufBezahlenDialog.tsx` in the same `tse-service` INTENTION→FINISH sandwich as `BezahlenDialog.tsx`, then `recordTseSignature` against the returned `transactionId`, with the identical offline-queue fallback. **Doctrine:** governance / KassenSichV §146a. **Verify:** a fiscal integration test asserts an Ankauf finalize produces a `tse_signatures` row; live-check a buy-in on the sim shows a signed receipt.
- **1.3a — Migrate the TSE queue to the durable outbox.** Move `tse-service.ts` off `localStorage['warehouse14.tse-queue.v1']` into the Tauri-SQLite outbox (`outbox-store.ts`), and **remove that key from the sign-out purge in `AppShell.tsx` `PER_OPERATOR_STORAGE_KEYS`**. **Doctrine:** governance / GoBD §146, honesty. **Verify:** unit test that an enqueued obligation survives a sign-out (it is now in SQLite, not per-operator localStorage).
- **1.3b — Drain the TSE queue.** Add a periodic drain that replays each entry to the fiscal API + `recordTseSignature` and removes on success, with `in_flight` transition and an attempt cap. **Doctrine:** governance / GoBD §146. **Verify:** unit test the drain (enqueue → drain → row recorded → removed).
- **1.3c — Surface the queue + a spec'd overflow policy (product decision).** Surface a persistent Gerätemanager alert while the queue is non-empty. The overflow behavior is a **product decision, not a bare code change**: past a hard threshold, do **not** silently block sales (that turns a Fiskaly outage into a self-inflicted denial of service); instead require a step-up **"Manager-Freigabe: weiterverkaufen"** that is itself recorded in the audit ledger, and never roll off the cap. **Doctrine:** governance, honesty, availability. **Verify:** live-check the alert appears; live-check that past the threshold a manager step-up is required and logged, and the till still sells after approval.
- **1.4a — Implement `pos_intents` write-before-network (self-contained).** Add a `PosIntentStore` (Tauri-SQL) that writes `pos_intents` **before** `transactionsApi.finalize`/`ankauf` in both fiscal dialogs, and marks `resolved_at` on success. **Doctrine:** correctness / GoBD §146 (the hole ADR-0044 was written to close). **Verify:** `grep pos_intents` now finds writers; unit test the write-then-resolve happy path.
- **1.4b — Startup reconcile routes into the outbox (names the dependency).** On startup, reconcile rows where `resolved_at IS NULL AND failed_at IS NULL` **into the durable outbox** (self-contained, Phase 1). Unreconcilable rows that need human judgement are flagged for the **Konfliktpostfach built in Phase 6.1** — until 6.1 lands they surface as a plain Gerätemanager count, so 1.4 fully lands in Phase 1 with no forward dependency for its verifiable core. **Doctrine:** correctness. **Verify:** integration test simulates a crash between confirm-tender and enqueue, restarts, and asserts the sale is recovered into the outbox.
- **1.5 — Populate the TSE VAT breakdown (signed body).** Extend `TseFinishParams` and `tse_finish_transaction` (`src-tauri/src/commands/tse.rs`) so `amounts_per_vat_id` carries the real per-VAT-rate cart totals instead of `[]`. **Doctrine:** governance / KassenSichV / DSFinV-K. **Verify:** a signed-body assertion test confirms the VAT decomposition is present for a mixed 19%/7%/§25a cart.
- **1.6 — Carry DSFinV-K process-data into TSE FINISH (signed body).** Carry the KassenSichV process-data into `tse-service.ts` FINISH and persist it on `tse_signatures` so `tse.csv` can emit the full signed payload. This is clustered here with 1.5 deliberately: both change the **signed** record and must land together. **Doctrine:** governance / inspection-grade. **Verify:** a signed-body test confirms process-data is present and persisted. **External-blocked gate (not an in-session sub-step):** Prüftool validation with the Steuerberater is a separate exit gate tracked in Phase 6; keep the non-certified honesty note in the UI until it passes.
- **1.7 — Fix the ZVT reversal parse (fiscal correctness).** Replace the naive single-read in `zvt_reverse_payment` (`commands/zvt.rs:211`, `buf.len() >= 8 && buf[6]==0x00 && buf[7]==0x00`) with the same full conversation-loop parser `zvt_authorize_payment` uses, so a Storno confirmation that arrives across multiple reads is not misread as failure. **Doctrine:** correctness / fiscal reversal. **Verify:** a protocol test feeds a fragmented reversal reply and asserts it is recognized.
- **1.8 — Move the ZVT amount guard into the real authorize path.** The `MAX_BCD_CENTS`/zero guard lives only in `mock/zvt_mock.rs:34`; add it to the real `zvt_authorize_payment` **before** BCD framing so an oversized/zero amount is rejected cleanly rather than producing a malformed on-wire frame that is green in mock. **Doctrine:** correctness, verification doctrine (protocol-faithful mocks). **Verify:** a test asserts real authorize rejects `> MAX_BCD_CENTS` and zero with a clean error, matching the mock.
- **1.9 — Schema-validated cart rehydration.** Add a `migrate`/`merge` to both `cart-store.ts` and `ankauf-cart-store.ts` that validates each persisted line against a typebox/zod schema (reuse the `TseQueueEntrySchema` pattern) and drops malformed lines instead of throwing into `computeLineMath`. **Doctrine:** correctness / honesty. **Verify:** unit test rehydrates a corrupt line and asserts a clean drop, not a white-screen.
- **1.10 — Honest TSE-outage receipt + real Bediener name.** Replace the four `'TSE Ausfall'` field substitutions (`BezahlenDialog.tsx`, `ReceiptPreview.tsx`) with one German outage banner ("TSE momentan nicht erreichbar — Signatur wird nachgereicht") and omit the QR/counter fields when unsigned; resolve the session actor's display name for `cashierName` instead of a UUID slice. **Doctrine:** honesty (b), (a). **Verify:** live-check receipt preview offline shows one banner, no `TSE Ausfall` in the QR slot, and a real Bediener name.
- **1.11 — Non-destructive discounts.** In `CartPanel.tsx`, warn before a cart-wide `InvoiceDiscount` overwrites an existing per-line Rabatt/reason, and distribute over post-per-line-discount bases. **Doctrine:** correctness. **Verify:** unit test that a pre-set line discount is preserved/merged; live-check the warning fires.

---

## Phase 2 — Mobile-to-desktop parity port (the doctrine spine)

**Goal:** route every desktop error/label through the shared spine from Phase 0, port the read-side offline layer, and eliminate the raw-token leaks — turning doctrine (a) into a compile-time guarantee the way mobile already has it.

**Exit criteria:** the Phase 0.4 grep-gate returns **zero** for its frozen regex set (gate flips to **failing-on-nonzero**); read surfaces show honest last-good data with a stale marker offline; entity-detail panels no longer flash the previous entity.

- **2.1 — Produce the enumerated sweep list, then route all errors through `describeError`.** First generate the exact `file:line` list from the Phase 0.4 regex set as a committed checklist artifact (no "e.g." — the ~89 sites across the two apps, e.g. `PinLogin.tsx:92`, `ProductSheet.tsx:999,1162,1471`, `ClosingsPanel.tsx:231-236`, `ApprovalsPanel.tsx:133`, `LagerPanel.tsx:108`). Then, **per file in one commit each**, replace `err.message`/`j.error.message` with `describeError(err)` from `@norns/i18n-de` **and** remove that file's `default:` branch in the **same** commit, running `pnpm -r typecheck` per commit so the exhaustive shared `Record` (already landed in 0.1) is the only path and typecheck never goes red mid-sweep. **Doctrine:** (a), (b), (c). **Verify:** the checklist is fully ticked; the grep-gate returns zero; `pnpm -r typecheck` green at every commit; live-check a forced 409 on Z-Bon finalize shows German.
- **2.2 — Wire `CONFLICT_TOKENS`/`describeConflict` on 409s.** Route the four Z-Bon closing conflicts (`ClosingsPanel.tsx`), KYC/eBay/Kunden conflicts through `describeConflict`. **Doctrine:** (a), (b). **Verify:** live-check each of the four finalize conflicts renders actionable German.
- **2.3 — Kill raw-token enum fallbacks (port, do not rebuild).** Replace every `LABELS[x] ?? x` / `humanizeEnum` fallback (`KundenPanel.tsx`, `TerminePanel.tsx`, `item-type-label.ts`, Aufgaben `task.priority` in `Aufgaben.tsx`, appraisal metal in `AppraisalItemForm.tsx`) with `germanLabel(registry, x, 'Unbekannt')`. **Reuse** mobile's existing `TASK_PRIORITY_LABEL` (`german-text.ts:418`) and the metal labels from mobile's `ankauf-ui.ts` via `@norns/i18n-de` — do **not** derive a second `PRIORITY_LABEL`/`METAL_LABEL`, which would re-create the exact drift this phase kills. **Doctrine:** (a). **Verify:** `pnpm -r typecheck` green; live-check Aufgaben shows "Dringend" and the appraisal hint shows "Gold".
- **2.4 — Consolidate the desktop label spine.** Replace the three duplicated `METAL_LABEL` copies (`LagerTable.tsx`, `CatalogGrid.tsx`, `Kurse.tsx`) and scattered item-type/status maps with imports from `@norns/i18n-de`. **Doctrine:** (a), anti-drift. **Verify:** grep finds one metal-label source; typecheck green.
- **2.5 — Port the read-side offline layer.** Create `tauri-pos/src/offline/` porting `read-cache`, `useCachedQuery` (with the `(key, updatedAt)` cache-poisoning guard), `useSafeRetry`, `StaleBadge`, `OfflineNotice`, and `classifyRetry` from `apps/mobile/src/warehouse14/offline/`. Migrate the key dashboard/finance/Lager/Kunden reads off bare `useQuery`; set `placeholderData: keepPreviousData` on paginated/filtered lists. **Note:** the read-cache snapshot is a **new PII sink** and must be registered in the Phase 3.8 erasure-completeness audit. **Doctrine:** (b), parity. **Verify:** live-check a simulated outage shows last-good numbers with a stale marker; entity-detail param flip no longer flashes the prior entity.
- **2.6 — Fix the channel-flags model.** In `LagerTable.tsx` drive the Online badge off `isPublishedToWeb && status==='AVAILABLE'` (per `products.ts:80-85`), splitting Laden/Online-Shop/eBay into three truthful indicators; in `WebSeoPanel.tsx` compute `isLive = isPublishedToWeb && status==='AVAILABLE'` with an honest "sichtbar sobald verfügbar" pending state. **Doctrine:** (b). **Verify:** live-check a published DRAFT shows the pending state, not "LIVE".
- **2.7 — Port live per-status inventory counts.** Port mobile's `availability-ui` + `useInventoryCounts` to the Lager status chips (`Lager.tsx`) and a Verkauf count strip. **Doctrine:** parity, honesty. **Verify:** live-check chips show real Verfügbar/Reserviert/Verkauft counts.

---

## Phase 3 — Customer, KYC, identity & total erasure

**Goal:** make a "saved Ausweis" real, cross-device, viewable, and eraseable; converge the two divergent KYC stores; wire the built-but-dead MRZ scanner; and prove that erasure is **total** — including the at-rest PII sinks the audit and Phase 2.5 introduce.

**Exit criteria:** the Kunden-akte capture reaches the server store and stamps `kyc_verified_at`; a captured ID can be viewed and deleted; erase (Art.17) purges every enumerated PII sink; MRZ auto-fills the GwG fields.

- **3.1 — Converge KYC capture onto the server store.** Repoint `KycCaptureModal.tsx` (opened from the akte) to `customersApi.addKycDocument` (+ optional `stampKyc`) so a saved Ausweis is server-visible, cross-device, compliance-visible, and eraseable — or make the local Rust vault an explicit offline sync-queue. **Doctrine:** correctness, governance. **Verify:** live-check that an akte capture stamps `kycVerifiedAt` and satisfies the GwG gate.
- **3.2 — Add local-vault delete.** Add a `delete_kyc_document` Tauri command (`kyc.rs`) + `deleteKycRecord` (`kyc-store.ts`); call from `KycLocalDocs.tsx`. **Doctrine:** governance / DSGVO Art.17. **Verify:** unit test the Rust unlink; live-check a vault file deletes.
- **3.3 — Add "Kundendaten löschen (Art.17)" to the akte.** Add a step-up-gated destructive action + German confirm to `CustomerDetailPanel.tsx` calling `customersApi.erase`, which also triggers the 3.2 local purge. **Doctrine:** governance / DSGVO. **Verify:** live-check erase anonymizes server-side and purges the local vault.
- **3.4 — View a server-stored ID from the akte.** Add a step-up "Ausweis anzeigen" using `customersApi.getKycDocumentImage` into an in-memory data URI (no persist, revoke-on-close, mirroring `KycLocalDocs`). **Doctrine:** governance completeness, honesty. **Verify:** live-check a passport captured in Ankauf is viewable in the customer file.
- **3.5 — Wire MrzScanner into KYC capture.** Mount `MrzScanner.tsx` in the KYC form (`Fotos.tsx`); on result prefill `documentNumber`/`expiry`/`nationality`/`issuingCountry` and surface `valid` (check-digit) + expiry as German warnings ("Prüfziffern ungültig" / "Ausweis abgelaufen"). Offer a live camera "Ausweis fotografieren" via `useCamera.captureBlob` with file-upload fallback. **Doctrine:** correctness (GwG accuracy), parity. **Verify:** live-check MRZ paste auto-fills and an expired ID warns.
- **3.6 — Chain-of-custody + orphan-file fix.** On capture, stamp `verified_by_user_id`/`verified_at` and `emitAudit`; on index-write failure, unlink the just-written vault file instead of leaving an orphan (`KycCaptureModal.tsx`). **Doctrine:** governance. **Verify:** unit test the orphan-cleanup path; live-check the verifier shows on the record.
- **3.7 — Source GwG thresholds from the server.** Load `GWG_IDENTITY_THRESHOLD_EUR`/`ANKAUF_STEP_UP_HINT_THRESHOLD_EUR` (`ankauf-thresholds.ts`) from `system_settings`/env at boot; fall back to the constant only offline. **Doctrine:** honesty. **Verify:** live-check the banner number matches the server setting.
- **3.8 — Erasure-completeness audit (the "erase is total" proof).** Enumerate **every at-rest PII sink** and prove each is purged or redacted by the Art.17 flow: (i) server records/anonymization, (ii) the Rust KYC vault (3.2), (iii) **temp invoice/preview PDFs** — verified: `print_a4` (`pdf.rs:94`) and `open_pdf_preview` (`pdf.rs:122`) write customer names and §25a data to `std::env::temp_dir()` and **never delete them**; add cleanup (delete after print, revoke-on-close after preview, and a boot-time sweep of stale `warehouse14-invoice-*`/`warehouse14-preview-*`), (iv) the **read-cache snapshots created in Phase 2.5**, (v) the outbox `last_error_json`. **Doctrine:** governance / DSGVO completeness. **Verify:** a documented checklist maps each sink to its purge path; unit test the temp-PDF cleanup; live-check that after an Art.17 erase no invoice PDF for that customer remains in temp and the read-cache entry is gone.

---

## Phase 4 — Hardware completeness & Windows print parity

**Goal:** wire the orphaned scale, fix the two Windows print regressions, stop leaking raw hardware errors, and keep device badges honest — so the till is complete on both OS targets before OTA.

**Exit criteria:** an operator can pull a weight from the scale into Ankauf/Bewertung; A4 + label printing works on Windows; `describeHardwareError` never leaks raw `details`; badges stay truthful mid-shift.

- **4.1 — Wire the USB scale.** Add a `WaageSection` to `GeraeteManager.tsx` (`list_scale_ports` dropdown + persisted port in `hardware-store`) and a "Wiegen" action in Ankauf/Bewertung calling `readWeight` to fill the grams field. **Doctrine:** completeness, honesty. **Verify:** live-check (mock hardware) a weigh-in fills the field.
- **4.2 — Windows branch for A4 printing.** Add a `#[cfg(target_os = "windows")]` arm to `print_a4` (`pdf.rs`) spooling the PDF via the Windows print API, mirroring the thermal `win_print::print_raw` pattern. **Doctrine:** Win/Mac parity. **Verify:** build on Windows (Phase 0.3 CI) and confirm an invoice prints.
- **4.3 — Windows branch for system-mode labels.** Add a Windows branch to `label.rs send_system` routing raw bytes through `win_print::print_raw`. **Doctrine:** Win/Mac parity. **Verify:** Windows build prints a label.
- **4.4 — Clean German hardware errors.** Rewrite `describeHardwareError` (`hardware-client.ts`) to return a self-contained German sentence per `HardwareErrorKind` with an actionable next step; drop raw `err.details` from the human string (log it only). Normalize `generate_invoice_pdf` (`pdf.rs`) to `HwResult<Vec<u8>>` so it flows through the union. **Doctrine:** (a). **Verify:** live-check a forced printer error shows a clean German sentence, no `lpr exited` / `Permission denied`.
- **4.5 — Stable-weight polling + tare.** Make `read_scale_weight` (`scale.rs`) poll for a Stable (`S S`) reply, reject/flag Dynamic (`S D`), and expose a tare command. **Doctrine:** correctness (fiscal payout accuracy). **Verify:** unit test rejects a `S D` reading.
- **4.6 — Mid-shift re-probe.** Add a low-frequency (60–120s idle) background re-probe in `useHardwareAutoConnect.ts` refreshing `lastReachable`/`lastCheckedAt`. **Doctrine:** honesty. **Verify:** live-check a dropped printer flips the badge without a manual check.

---

## Phase 5 — Governance surface: control-desktop front door & completeness

> **Sequencing note (absorbing critique D4):** control-desktop is dead-on-launch today. Sub-step **5.1 (the login gate) is independent of the fiscal work and is pulled forward to run in parallel with Phase 1** — it is scheduled for delivery early, not fifth. The rest of Phase 5 (ledger completeness, oversight) depends on the Phase 0 spine and the Phase 2 sweep, so it stays here. If 5.1 cannot start early for capacity reasons, control-desktop is explicitly documented as known-broken until it lands, and that window is stated, not hidden.

**Goal:** give control-desktop a login, route its labels through the spine, resolve UUIDs to names, and fill the missing oversight workflows — turning a thin shell into an ideal management & governance surface.

**Exit criteria:** control-desktop establishes a session on launch and recovers on 401; the compliance ledger renders German labels + names + a chain-verify badge; the oversight surfaces that have real endpoints exist; exports self-heal step-up.

- **5.1 — Build the control-desktop PinLogin gate (delivered early, parallel to Phase 1).** Port the `tauri-pos` `AuthFlowCoordinator`/`PinLogin` into `control-desktop/main.tsx`: call `authPin.login`, hold session state, wrap `App` behind an authenticated boundary, add a global `UNAUTHORIZED → login` handler. **Doctrine:** security (P0 front-door gap, top-risk #3). **Verify:** live-check a fresh launch shows the PIN pad, authenticates, and reads the Bridge without 401s.
- **5.2 — German labels + name resolution on the ledger.** Rebuild `KonformitaetPanel.tsx` to render `describeError`/`audit-vocab` German labels + categories (from the 0.2 split) and resolved actor/entity **names** instead of raw `eventType`/`entityTable`/`actorUserId.slice`. Resolve `TerminePanel.tsx` customer/staff UUIDs to names. **First verify the endpoint shape (critique F4):** confirm the ledger/appointments endpoints can return resolved names; if they cannot, the server join is a **named prerequisite API task**, not an assumed capability. **Doctrine:** (a), (b). **Verify:** endpoint returns names; live-check the ledger shows German event names and human names, zero dotted tokens.
- **5.3 — Chain-verify action + `lastChainVerifiedAt`.** Add an ADMIN `GET /api/ledger/verify` calling `verify_ledger_chain()`, plus a "Kette prüfen" button and a `lastChainVerifiedAt` indicator in `KonformitaetPanel`. **Doctrine:** (b) — the "fälschungssicher" claim must be demonstrable. **Verify:** live-check the button returns an intact/broken-at-row result.
- **5.4 — Ledger pagination + filters.** Add cursor pagination + event-type/date/entity filters to the `/api/ledger` view. **Doctrine:** governance (Kassen-Nachschau). **Verify:** live-check paging back through history and filtering by event type.
- **5.5 — Route exports/finalize through the ApiClient.** Replace the raw `fetch()` DATEV/DSFinV-K/register/finalize calls (`ClosingsPanel.tsx`, `KonformitaetPanel.tsx`) with client methods so a 403 `STEP_UP_REQUIRED` opens `StepUpModal` and replays. **Doctrine:** governance, UX consistency. **Verify:** live-check a step-up-required export opens the PIN modal and re-issues the download.
- **5.6 — DSGVO erase on the governance surface.** Add a step-up-gated "Recht auf Löschung (Art.17)" action + KYC delete/replace to `KundenPanel`, wired to `customersApi.erase` / `deleteKycDocuments`, honestly shell-state-labelled, and triggering the same total-erasure path proved in 3.8. **Doctrine:** governance / DSGVO. **Verify:** live-check erase and ID delete/replace from the governance app; the 3.8 sinks are all purged.
- **5.7 — Fill the oversight gaps (five endpoint-gated sub-steps, not one bullet).** Each surface is its own sub-step gated on "does the endpoint exist?":
  - **5.7a — DLQ / worker-health** surface (if a health/DLQ endpoint exists; else a named server task first). **Verify:** reads a live endpoint, typecheck green.
  - **5.7b — TSE-certificate status + renewal action** against the existing TSE endpoint. **Verify:** shows cert validity and a German expiry warning.
  - **5.7c — Anomaly / AML-alert review** surface. **Verify:** reads live alerts, German labels.
  - **5.7d — Shift / Kassensturz oversight.** **Verify:** reads live shift data.
  - **5.7e — Device mTLS revocation** — the audit records this has **no endpoint** ("explicitly deferred"). This is therefore **server work first**; the desktop sub-step is blocked until the endpoint exists and is called out as such, not shipped as a dead button. **Verify:** blocked-gate documented; ships only once the endpoint lands.
- **5.8 — Four-eyes approvals with real strong-auth (promoted to a named sub-step).** Replace the `window.confirm` stub on the high-value approval queue with the real step-up/WebAuthn flow. A JS confirm dialog standing in for the second pair of eyes is a **fake control** on a regulated shop and ranks with the login gap. **Doctrine:** governance / four-eyes. **Verify:** integration test that a CASHIER **cannot self-approve**; approval requires a **fresh step-up recorded in the audit ledger**; live-check the step-up modal fires and the ledger row appears.
- **5.9 — Live connection pill + honest step-up bridge.** Probe `/health` on mount + interval so the header pill is never "unbekannt" (`App.tsx`); clean up the empty-token `stepUpService.ts` bridge to be truthfully session-based. **Doctrine:** honesty, correctness. **Verify:** live-check the pill is live from launch.

---

## Phase 6 — Roles, capability model & audit hardening

**Goal:** move from three coarse roles to a capability layer for least-privilege, add the compliance/conflict surfaces the shell already points at, and finish the audit-trail story honestly.

**Exit criteria:** sensitive actions are gated by named capabilities; the HealthDot conflict action resolves to a real Konfliktpostfach; the shell shows the signed-in operator; the un-chained `audit_log` is either surfaced/anchored or documented as an accepted limitation.

- **6.1 — Build the Konfliktpostfach (Compliance Inbox).** Create the surface listing outbox `conflict` rows (path, serverCode, `last_error_json`, `enqueued_at`) with resolve actions (verwerfen / erneut senden after review); add `markResolved` to `outbox-store.ts`; register `/compliance-inbox` in `surface-registry.ts` and point `HealthDot.tsx` at it (fixing the wildcard-swallowed dead route). This is the **"review surface"** referenced by 1.4b — the unreconcilable `pos_intents` rows now land here. **Doctrine:** governance (a halted conflict currently freezes the FIFO queue forever). **Verify:** live-check a seeded conflict appears and resolving it unblocks the queue; a 1.4b flagged intent appears here.
- **6.2 — Capability layer above the 3 roles.** Introduce a role→capability map (issue-refund, run-erasure, export-DSFinV-K, resolve-approval) checked server-side in `auth-policy.ts`; keep ADMIN/CASHIER/READONLY as presets; add a roles/permissions admin surface to control-desktop. **Doctrine:** governance / least-privilege. **Verify:** integration test that a CASHIER without `run-erasure` is refused; live-check the admin surface.
- **6.3 — Signed-in operator identity in the POS shell.** Add a compact operator chip/menu to `AppShellHeader.tsx` reading `session-store`, with sign-out reachable from it. **Doctrine:** governance, honesty. **Verify:** live-check the header shows "Angemeldet: <name>".
- **6.4 — Outbox lifecycle enforcement.** Add a retention pruner (delete expired non-fiscal `succeeded` rows, **never** conflict/fiscal), an `in_flight` transition during replay, and a `failed_terminal` path after an attempt cap; back off the 5s stats heartbeat when idle (`offline-replay.ts`, `outbox-store.ts`). **Doctrine:** governance/operational. **Verify:** unit test the pruner never touches fiscal rows.
- **6.5 — `audit_log` chain honesty (absorbing critique B4).** The security `audit_log` (logins, role changes, AML alerts) is **not** hash-chained and has no desktop surface. For "ideal governance" this cannot be silently implied as covered by 5.3 (which verifies only the fiscal ledger). Deliver **at minimum** a read-only desktop surface for `audit_log`; **preferably** a daily anchor (hash of the day's rows written into the hash-chained ledger). If neither is scoped for this release, the Definition of Done records it as an **explicit, accepted, documented limitation** — never as "covered." **Doctrine:** honesty (b), governance. **Verify:** either the surface/anchor is live, or the DoD carries the written limitation.
- **6.6 — DSFinV-K Prüftool validation gate (external-blocked).** With the Steuerberater, validate the full signed payload (1.5 + 1.6) against the official Prüftool. This is an **external-dependency exit gate**, not an in-session sub-step. **Doctrine:** governance / inspection-grade. **Verify:** Prüftool validation passes; keep the non-certified honesty note in-app until it does.

---

## Phase 7 — Shell polish, security hardening & missing UX

**Goal:** fix the shell bugs a live operator hits, close the remaining security TODOs, and finish the workshop/calendar/tasks UX — the "complete cashier + ideal management" polish that isn't fiscal but is doctrine-facing.

**Exit criteria:** one theme system; no dummy VAT can print; token in keychain; SSE via ticket; unified calendar; enriched Aufgaben/booking; house-style confirm dialogs.

- **7.1 — Unify the theme system.** Collapse `state/theme-store.ts` (`w14.theme`) and `lib/theme.ts` (`warehouse14.theme`) to one store + one key; make `ThemeToggle`, `Cmd+Shift+D`, boot init, and PinLogin all read/write it and subscribe (not seed-once). **Doctrine:** correctness. **Verify:** live-check the toggle and keybinding agree and boot paint matches.
- **7.2 — Kill the dummy VAT id.** Replace `SHOP_INFO.vatId`/phone dummies (`shop-info.ts`) with real values from `system_settings`, and make an unresolved/empty `vatId` a hard **"receipt locked"** state rather than printing `DE123456789`. **Doctrine:** honesty (b) / GoBD. **Verify:** live-check a receipt with unresolved shop-info locks instead of printing a fake VAT id.
- **7.3 — Token to keychain + SSE ticket.** Move the session bearer from plaintext localStorage (`session-token.ts`) to the Tauri OS keychain (mirror the Fiskaly pattern); replace the SSE `access_token` URL param (`useLedgerStream.ts`) with a short-lived stream ticket; reconsider `sql:allow-execute` in `capabilities/default.json`. (The production-CSP `localhost` drop already landed in Phase 0.6.) **Doctrine:** security. **Verify:** grep confirms no plaintext token; SSE connects via ticket.
- **7.4 — Unify the calendar.** Make the appointments-DB FullCalendar cockpit the single home calendar on `/werkstatt` (promote `/termine` to primary or embed it), retiring the parallel `/api/calendar` Google list so a real booking can't be missed. Deepen `QuickCreateDialog` with a staff picker, a customer picker, and an `availableSlots`-backed free-slot picker. **Doctrine:** correctness, honesty, completeness. **Verify:** live-check a booking made in the cockpit appears on the home screen.
- **7.5 — House-style confirm dialogs + Aufgaben/product decision.** Replace both `window.prompt` cancel-reason flows (`TerminDetailSheet.tsx`, `Aufgaben.tsx`) with a `ModalShell` reason form; add due-date/assignee/related-entity editing to Aufgaben; make the product decision on "Werkstatt" (build a repair-order pipeline or rename to Übersicht/Zentrale). **Doctrine:** UX quality, honesty. **Verify:** live-check the styled reason form and enriched task editor.
- **7.6 — Small robustness fixes.** Pass `photoCount` into `deriveLifecycleStage` (`ProductSheet.tsx`), reuse `classifyScanMatch` on the Lager scan path, fix "Hinzufügt…" → "Wird hinzugefügt…", demote raw `W14-` codes in HealthDot to a muted support line. **Doctrine:** (a), honesty. **Verify:** live-check each.

---

## Phase 8 — Release, signing & OTA (Windows + Mac)

**Goal:** ship one coordinated, signed, two-app release with a proven end-to-end OTA loop on both platforms. Last phase; depends on every prior phase being green.

**Exit criteria:** one workflow builds and releases both apps with signed manifests; a second installed build actually picks up an update on both Windows and Mac; the env trap and canonical-version guard are provably in force.

- **8.0 — Bump `plugin-updater` and align the Tauri API (moved here from Phase 0).** Bump `apps/control-desktop` `@tauri-apps/plugin-updater` 2.5.1 → 2.10.1 to match POS; align `@tauri-apps/api`/`cli`. This is an **auto-update-path behavior change** and is deliberately co-located with the OTA rehearsal that tests it end-to-end. **Doctrine:** (c), parity. **Verify:** both apps build (Phase 0.3 CI); the bump is exercised by the 8.8 rehearsal.
- **8.1 — Port the two-app pipeline (port → prove → delete, in that order).** Do **not** "replace and delete" in one move. (a) **Port** the `build-pos`, `build-control`, and `control-updater-manifest` jobs from `infrastructure/ci/release.yml` into `.github/workflows/` (the active file has **zero** control references — verified). When assembling `latest-control.json`, resolve download URLs from the **real GitHub release assets API**, not a regex-reconstructed basename. (b) **Prove** a throwaway tag builds both apps and both manifests. (c) **Only then delete** the `infrastructure/ci` copy to remove the duplicate-name foot-gun. **Doctrine:** governance (control OTA currently 404s forever), (d). **Verify:** the throwaway tag produces both bundles + both manifests before the infra copy is removed.
- **8.2 — Close the env trap in the active workflow.** Set `VITE_API_BASE_URL=https://api.warehouse14.de` in the POS build step (the infra file has it, the live one doesn't — verified); add a post-build assert that the shipped `dist` contains `api.warehouse14.de` and **not** `localhost:3001`. **Doctrine:** honesty (b). **Verify:** the assert fails a deliberately-mis-built bundle and passes the real one.
- **8.3 — macOS Developer ID + notarization (with hardened runtime).** Add `APPLE_CERTIFICATE`/`APPLE_ID`/`APPLE_TEAM_ID` secrets and wire tauri-action signing + `notarytool` for both apps (replacing `signingIdentity "-"`). **Notarization also requires hardened runtime + entitlements** — explicitly confirm hardened-runtime signing and the entitlements the **keychain and SQL plugins' helper binaries** need, or the notarize step passes locally and fails in CI. **Doctrine:** governance (silent OTA on Mac). **Verify:** a notarized bundle launches on a clean Mac with **no** Gatekeeper prompt; `codesign --verify --deep` and `spctl -a` pass.
- **8.4 — Windows Authenticode signing.** Add an Authenticode (or Azure Trusted Signing) cert and set `bundle.windows.certificateThumbprint` + signing env in CI for both apps. **Doctrine:** governance (SmartScreen on every update). **Verify:** the installer runs without a SmartScreen warning.
- **8.5 — Canonical-version guard as a required check + endpoint/prerelease discipline.**
  - Wire the Phase 0.5 guard (`tag == tauri.conf.version`, tauri.conf canonical) as a **required status check on the release workflow**, so a mismatched tag cannot release.
  - Confirm both `tauri.conf.json` updater endpoints resolve (POS `latest.json`, control `latest-control.json`) after CI substitution; confirm the `__GITHUB_OWNER__` placeholders are substituted.
  - **Audit existing GitHub releases' prerelease flags** before the big tag: the `v1.0.0-rc1..rc3` tags exist and `releases/latest` excludes prereleases — verify none was ever published as a full release (which would already poison "latest"). Keep production tags `prerelease:false` going forward. **Doctrine:** (d). **Verify:** the required check blocks a bad tag; the prerelease audit is documented clean.
- **8.6 — Verify the shared minisign key across both manifests.** POS `latest.json` and control `latest-control.json` are signed by the **same** updater key from one CI secret. Assert that the **control pubkey in `control/tauri.conf.json` matches the CI signing key** — if the infra pipeline ever used a different key, control updates fail minisign verify silently. **Doctrine:** (d). **Verify:** a signed control manifest verifies against the control pubkey.
- **8.7 — Port the unified update client to control.** Port `useAppUpdate.ts` + `UpdateCenter.tsx` + `UpdateButton.tsx` (with the open-sale/open-work guard) to control-desktop and delete its divergent auto-relaunching `UpdateBanner.tsx`. **Doctrine:** parity. **Verify:** control shows the unified German modal with an explicit relaunch.
- **8.8 — German curated release notes + post-release smoke check.** Generate manifest `notes` from a German CHANGELOG section for the tagged version so `UpdateCenter` shows curated German. Add a post-release smoke check that GETs each resolved `latest.json`/`latest-control.json` and asserts 200 + valid minisign. **Doctrine:** (a), (d). **Verify:** in-app notes are German; both manifest GETs pass.
- **8.9 — Full OTA rehearsal (the evidence).** Cut a real throwaway tag through the workflow; install build N on a Windows box and a Mac; publish N+1; confirm **both apps** auto-detect, download, verify minisign, and relaunch on **both** platforms. **Doctrine:** (d) — never claim working OTA without this. **Verify:** documented before/after version on all four targets (2 apps × 2 OS).

---

## Checkpointed rollout strategy (never break the live till)

The live till (Intel iMac, `fiscal_active` per the go-live plan) must keep selling through every phase. Rules:

1. **Additive-first, behind the same green gate.** Every sub-step ends with `pnpm --filter @norns/db build && pnpm -r typecheck` **plus** the Phase 0.3 desktop build. No sub-step merges without both green (doctrine c).
2. **Live-verify on the simulator, build only on command.** Iterate on the Mac-local sim per every sub-step's live-check; **do not build the `.ipa`/`.dmg`/`.exe` until Basel explicitly commands it** (build-on-command rule). Native-dep features (scale, keychain, Windows print, notarization) are the only build-phase exceptions, and those get a build only when the phase reaches its OTA rehearsal.
3. **Reversible ordering by risk.** Phase 0 changes nothing rendered (the `plugin-updater` behavior change was moved out to Phase 8). Phase 1 (fiscal) lands as isolated, individually-revertible commits with their own tests; the signed-body cluster (1.5–1.8) is co-located so the signed record never ships half-done. Parity (Phase 2) flips the grep-gate to failing **only after** the last leak is fixed, and each 2.1 file does body-replace-and-remove-`default:` in **one commit** with a per-commit typecheck so the exhaustive `Record` is always the only path — typecheck never goes red mid-sweep.
4. **P0 gaps are not schedule-buried.** Control-desktop login (5.1) is delivered **early, parallel to Phase 1**, or its broken window is documented. Four-eyes strong-auth (5.8) is a named sub-step, not a clause.
5. **The OTA cutover is the single riskiest moment — rehearse it on a throwaway tag first (8.9) before the real "big update" tag.** The port-prove-delete order (8.1) means the only working release path is never deleted before its replacement is proven. Keep `prerelease:false` discipline so `releases/latest` never resolves to an rc.
6. **No claim without evidence (doctrine d).** Each phase's exit criteria are the evidence contract: a passing test **and** a named live-check on the sim (or, for signing/OTA, an install on the real OS). External-blocked gates (Prüftool 6.6, device-revocation endpoint 5.7e) are tracked as blocked, never silently marked done.

---

## Definition of done (whole update)

- **Cashier is complete:** mixed-treatment carts rejected; Ankauf TSE-signed; scale wired into weigh-in; receipts print on Windows and Mac; no `TSE Ausfall` field-leak; real Bediener name; ZVT reversal parses via the full loop and authorize rejects oversized/zero amounts before framing. *(Card acquirer abstraction — SumUp/Mollie — is **not** in this release; it is recorded as a known deferred feature, not asserted as done, since no phase builds it.)*
- **Fiscal loss is impossible:** TSE queue is durable + drained + survives sign-out; the overflow policy requires a logged manager step-up rather than silently blocking or dropping; `pos_intents` write-before-network + startup reconcile is live; TSE FINISH carries the VAT breakdown **and** process-data; DSFinV-K validated against the Prüftool (external gate 6.6 passed, or the non-certified honesty note remains).
- **Doctrine (a) is compile-time-guaranteed on desktop:** the Phase 0.4 grep-gate returns zero for its frozen regex set; every enum/status/conflict/error flows through `@norns/i18n-de` with a German `Unbekannt` fallback; zero raw tokens on any panel including `KonformitaetPanel`; `audit-ui` is split into portable vocabulary + per-platform presentation, and the package has zero React-Native imports.
- **Doctrine (b) holds:** read surfaces show honest last-good + `StaleBadge` offline; channel flags are truthful; no dummy VAT can print; chain-verify is demonstrable; the production CSP no longer allows `localhost`.
- **Erasure is total:** Art.17 purges server records, the Rust KYC vault, temp invoice/preview PDFs, read-cache snapshots, and outbox `last_error_json`, each proved by the 3.8 checklist.
- **Governance is ideal:** control-desktop has a login front door; ledger shows German labels + names + pagination + a chain-verify badge; DSGVO erase + KYC delete/replace on both apps; the oversight surfaces with real endpoints exist (DLQ, TSE-cert, anomaly, shift), device-revocation tracked as endpoint-blocked; capability layer + Konfliktpostfach; **four-eyes approvals gated by a fresh step-up recorded in the ledger** (no `window.confirm` control); the un-chained `audit_log` is surfaced/anchored or documented as an accepted limitation (6.5).
- **Parity is closed:** every cited mobile fix (`describeError`/`CONFLICT_TOKENS`/`germanLabel`/`TASK_PRIORITY_LABEL`, `useCachedQuery` + cache-poisoning guard, `audit-vocab`, erase/KYC-delete, availability counts, env-trap discipline) is ported, reusing mobile's registries rather than re-deriving them.
- **One big signed release with working OTA (doctrine d):** a single workflow ships both apps, macOS notarized with hardened runtime + Windows Authenticode-signed, both `latest.json`/`latest-control.json` resolve with valid minisign against the correct pubkeys, the canonical-version guard is a required check, and a real N→N+1 update has been observed installing on Windows **and** Mac for **both** apps.
- **The gate is green and the live till never broke:** `pnpm --filter @norns/db build && pnpm -r typecheck` + the desktop build pass on the release tag, and the shipped bundle provably points at `api.warehouse14.de`.

---

**Verified evidence (this revision):** `apps/mobile/src/warehouse14/audit-ui.ts:36` imports `lucide-react-native`/`BadgeProps` (not api-client-clean); `apps/mobile/src/warehouse14/german-text.ts` imports api-client only and has `TASK_PRIORITY_LABEL:418`; `apps/tauri-pos/package.json` version `0.1.0` vs `apps/tauri-pos/src-tauri/tauri.conf.json` `0.4.33`; `plugin-updater` POS `2.10.1` vs control `2.5.1`; **89** `err.message`/`error.message`/`j.error.message` sites across both desktop apps (excluding tests); `.github/workflows/release.yml` has **0** `control` references and no `VITE_API_BASE_URL`, `infrastructure/ci/release.yml` exists; `apps/control-desktop/src-tauri/src/../main.tsx` mounts session-less; `apps/tauri-pos/src-tauri/src/commands/zvt.rs:211` naive reversal read vs authorize loop; `MAX_BCD_CENTS` guard only in `apps/tauri-pos/src-tauri/src/mock/zvt_mock.rs:34`; `apps/tauri-pos/src-tauri/src/commands/pdf.rs:94,122` write PII PDFs to `temp_dir()` with no cleanup.  ⟵ Quellen inzwischen geloescht, Portierung war abgeschlossen